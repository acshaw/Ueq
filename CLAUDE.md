# Ueq — Design Doc

## Project Overview
Unity URP multiplayer game using Mirror networking. Third-person-style with click-to-target combat. Currently in early prototype phase.

## Tech Stack
- **Engine**: Unity (URP)
- **Networking**: Mirror (KCP/UDP transport)
- **Input**: Unity Input System (polling, not callbacks)
- **Scene**: `Assets/Scenes/SampleScene.unity`
- **Editor tool**: `Tools/Setup Player Scene` bootstraps the scene from scratch

## Architecture

### Player
- `NetworkedPlayer` (Mirror `NetworkBehaviour`) — authoritative movement + look + targeting
  - RMB held = look mode (cursor locked); LMB click = target selection
  - `CmdSendInput` syncs move/yaw/sprint/jump to server each frame
  - `CmdAttack` is stubbed — no damage logic yet
- `PlayerController` — standalone (non-Mirror) fallback for local testing
- `Health` — generic health component; `SyncVar` + events for UI/VFX hooks; `EffectiveMax` uses class HP formula for players, mob definition for enemies, serialized fallback otherwise; `RefreshMax()` called on level-up or race/class change
- `PlayerAutoAttack` — key 1 toggles autoattack; derives ATK from STR/DEX weighted by `WeaponCategory` (Might/Finesse); hit roll shifted by (ATK-AGI)×shiftFactor; Glancing/Normal/Solid multipliers with variance; miss sends combat chat message; Critical fires as Solid until skill system exists
- `PlayerMana` — mana pool NetworkBehaviour; `SyncVar` current/max; `ComputeMax()` uses INT or WIS per class `ManaStatType`; `UseMana`/`RestoreMana`/`RefreshMax`; zero max for classes with `ManaStatType.None`
- `PlayerRegen` — server-side HP + mana regen; 1/tick every 6s per-player; sitting (2/tick) deferred; only ticks when alive and below max
- `PlayerAbilities` (NetworkBehaviour) — hotbar (8 slots, keys 2–9), known abilities, GCD + linked-timer cooldown engine; `SyncList<string>` hotbar + known; `SyncList<float>` per-slot cooldown for UI; `TryCast` validates range/mana/cooldown then calls effects in sequence; `SetRaceClass` populates known abilities from `ClassDefinition.startingAbilities`
- `AbilityDefinition` (ScriptableObject, `Assets/Ueq/Ability Definition`) — id, name, targetingType, range, castTime, manaCost, `List<AbilityTag>` tags (semantic), `List<CooldownLink>` cooldownLinks (empty = GCD), `List<AbilityEffect>` effects; assets in `Resources/Abilities/`
- `AbilityTag` (ScriptableObject, `Assets/Ueq/Ability Tag`) — pure semantic label (tagId, displayName); used in cooldown links and future systems (combos, elemental typing); assets in `Assets/ScriptableObjects/AbilityTags/`
- `CooldownLink` (serializable struct) — `{AbilityTag tag, float duration}`; when ability cast, starts shared timer for that tag; ability on cooldown if any linked tag timer > 0; empty links → GCD applies instead
- `AbilityEffect` (abstract ScriptableObject) — `Apply(caster, target, source)`; concrete: `DamageEffect` (baseDamage + stat scaling → `Health.TakeDamage`), `HealEffect` (baseHeal + stat scaling → `Health.Heal`); both support `ScalingStatType` enum
- `AbilityRegistry` (MonoBehaviour singleton) — loads `AbilityDefinition` assets from `Resources/Abilities/`; `Get(id)` lookup
- `HotbarUI` / `HotbarSlotUI` — 8-slot bar bottom-center; name label + cooldown countdown + grey overlay per slot; `Tools/Setup All` creates `HotbarCanvas`
- **Ability Editor** — `Tools/Editor/Ability Editor`; left panel lists assets; right panel: Identity, Targeting, Resource, Tags, Cooldown Links, Effects sections; creates in `Resources/Abilities/`; warns if not in Resources
- **Ability Tag Editor** — `Tools/Editor/Ability Tag Editor`; CRUD for `AbilityTag` assets in `Assets/ScriptableObjects/AbilityTags/`
- Race & Class Editor gains **Known Abilities** section on Classes tab (backed by `ClassDefinition.startingAbilities`)

### Enemy
- `Enemy` (`NetworkBehaviour`) — requires `Health` + `Targetable`; drives the AI state machine
- `EnemyAI` — server-only state machine (Idle → Chase → Combat → Return); uses NavMeshAgent
- `Targetable` — attach to anything selectable; `SetHighlight()` tints via `MaterialPropertyBlock`

### Combat
- `MobDefinition` (ScriptableObject) — all mob data in one asset: identity, combat, movement, AI, faction
- `MobApplicator` — component on Enemy prefab; holds `MobDefinition` ref; sets GameObject name + NavMeshAgent speed in `Awake`
- `Health`, `EnemyAI`, `NpcFaction`, `NpcEventDispatcher` each cache a `MobApplicator` ref in `Awake` and pull values from the definition (priority: MobDefinition → own serialized field)
- `NetworkedPlayer.ServerTarget` (NetworkIdentity) — set via `CmdSetTarget` on target change; server-side read point for autoattack
- `MobKillReward` (NetworkBehaviour, `IOnDeath`) — on mob death, awards `xpReward` XP directly to the killing player; Reward channel chat messages; fires after `Corpse.OnDeath`
- **Mob Editor** — `Tools/Mob Editor` EditorWindow; left panel lists all `MobDefinition` assets, right panel edits in sections; creates assets in `Assets/ScriptableObjects/Mobs/`
- **Race & Class Editor** — `Tools/Race & Class Editor` EditorWindow; three tabs: Races, Classes, XP Table; list + editor per tab; inline modifier summary ("20% more XP required"); XP Table tab: scrollable per-level grid (XP to complete + running cumulative), Reset to Defaults button; creates race/class assets in `Assets/ScriptableObjects/Races|Classes/`; XP Table asset lives at `Assets/Resources/XpTable.asset` (loaded via `Resources.Load`); "Create Default XP Table" button bootstraps it if missing
- `XpTableDefinition` (ScriptableObject) — holds `int[] xpPerLevel`; `PlayerExperience` loads it from Resources at runtime, falls back to `DefaultValues` if asset is absent

### Spawn System
- `SpawnTimer` (ScriptableObject) — `baseSeconds` + `variance`; `Roll()` returns randomized delay; shareable across spawn points
- `SpawnTable` (ScriptableObject) — weighted list of `SpawnTableEntry` (mob + weight + groupSize hook); `Roll()` picks an entry; holds a `defaultTimer`
- `SpawnPoint` (MonoBehaviour) — placed in scene; polls every 5s for players within `activationRadius`; spawns on activation, respawns after timer on mob death; inactive points pause respawn (mob spawns on next player arrival)
- `MobDefinition.prefab` — the base prefab to instantiate for this mob type
- `MobApplicator.SetDefinition()` — called by SpawnPoint after Instantiate, before NetworkServer.Spawn, so OnStartServer reads the correct definition
- `EnemyAI` resolves its values in `OnStartServer` (not Awake) to guarantee SetDefinition has already run

### Inventory
- `ItemDefinition` (ScriptableObject, `Assets/Ueq/Item Definition`) — `itemId`, `displayName`, `description`, `maxStackSize`; `IsStackable` computed property
- `InventorySlot` struct — `itemId` + `quantity`; Mirror NetworkWriter/NetworkReader extensions in `InventorySlotSerializer`
- `ItemRegistry` (MonoBehaviour singleton) — loads `ItemDefinition` assets from `Resources/Items/` at runtime; `Register(def)` for editor-time additions
- `PlayerInventory` (NetworkBehaviour) — `SyncList<InventorySlot>` (8 slots), SyncVar currency fields; `AddItem`/`RemoveItem`/`HasItem`/`IsFull`; `AddCurrency`/`SpendCurrency` (10:1 PP→GP→SP→CP normalization)
- `InventoryUI` — B key toggle (suppressed when chat open); late-binds to local player's `PlayerInventory` on first Update; refreshes slot labels + currency line via `SyncList.Callback`

### Network
- `GameNetworkManager` extends `NetworkManager`; logs connect/disconnect; spawn points via `NetworkStartPosition`
- Player prefab: `Assets/Prefabs/Player.prefab`

### Zones (M3.0 / 3.0.2)
- **Model:** single-server, multi-scene. Each zone is an additively-loaded scene authored at a distinct **world-space offset** (creslins_field=0 [base scene], thornwood=+5000, grukmars_deep=+10000); the base scene stays the active `SampleScene`. `ZoneInterestManagement : SceneInterestManagement` partitions observers by scene (players in different zones don't see each other/mobs). Player/mob `NetworkTransform` sync in **World** coordinate space.
- `ZoneCatalog` (ScriptableObject in `Resources/`) — boot list of `ZoneDefinition` (zoneId → sceneName → worldOffset → isBaseScene). Not DB-backed (a zone's scene is a Unity build).
- `ZoneManager` (on the NetworkManager GO, server-only) — loads the catalog, additively loads non-base zones, indexes each zone's entries + portals, polls portals for player proximity, and performs transitions. `ServerMovePlayer(conn, zoneId, entryId)` = `MoveGameObjectToScene` → client `SceneMessage` (additive load/unload) → `NetworkTransform.ServerTeleport` → chat-grid resync; supports **intra-zone** teleports (same-zone target warps without a scene swap). `ServerPlaceInZone(...)` does login/respawn placement into a persisted zone.
- `ZonePortal` (in-scene marker) — proximity portal (server poll, not a physics trigger — spike finding); `targetZoneId` + `targetEntryId` + `radius`; spawns a cosmetic cyan pillar at runtime. `ZoneEntry` — named arrival point (`entryId`); its transform position **and yaw** set the arrival spot + facing (NWN-style waypoint). Both are drag-in prefabs (`Assets/Prefabs/Zones/`) with Scene-view gizmo labels.
- **Zone-aware systems:** `characters.zone_id` persists (migration 0017) → login spawns into the saved zone; `SpawnPoint` moves spawned mobs into its own zone scene; `PlayerCorpse` + death-respawn are zone-aware; `ChatManager` delivers via `conn.Send` (survives interest partitioning). Baked NavMesh per zone is **persisted as an asset** so it reloads with the additive scene.
- **Authoring:** `Tools/Zones/Build Zone Scenes` (builds the 3 MVP zones + linear portal graph), `Tools/Zones/New Zone…` (stamp a new flat zone), `Tools/Zones/Create Zone Prefabs`. Generalized `ZoneSetup.BuildFlatZone(...)`. Zone **content/decoration is 3.1** (current zones are flat scaffolds ~280u each).

## Systems Design

### Faction System

**Model:** Named thresholds on a numeric scale, EQ-style. Thresholds defined in data (ScriptableObject) so new standings can be added without code changes.

Default thresholds (low → high standing):
`KOS → Threatening → Dubious → Apprehensive → Indifferent → Amiable → Kindly → Warmly → Ally`

**Player faction scores:**
- Every player has a numeric score for every faction in the game
- Scores initialize at character creation from a **race defaults table** (e.g., Troll + Qeynos Guards → -10,000; Dwarf + Qeynos Guards → -1,000)
- Scores are **per-player** — two trolls can diverge through individual choices over time
- Default for any faction not covered by the race table: **Indifferent (0)**

**NPC faction membership:** each NPC belongs to **one faction**

**Evaluation:** when an NPC perceives a player → look up player's score for this NPC's faction → map to threshold → drive behavior

**Faction hit sources:** kills, quests, items, dialogue choices, spells, abilities

**NPC-to-NPC interaction:** faction-level ally/neutral/hostile lists (not the numeric score system) — used for patrol behavior, guards responding to nearby fights, etc.

**Disguise / Illusion:**
- Players can have an apparent race/form separate from their actual one (bard masks, enchanter illusions)
- Faction checks use apparent identity — the NPC evaluates based on what it perceives
- Illusion is a clean identity substitution: the check uses the illusioned race's default score for that faction, ignoring the player's earned score entirely. The masked player is treated as an unrecognizable person who happens to measure as that race.

**Perception check (OnPerceived):**
1. Determine player's apparent race (accounting for active illusions)
2. Look up player's score for this NPC's faction
3. Map score to named threshold
4. Drive behavior: KOS/Threatening → aggro; Dubious/Apprehensive → warning; Indifferent+ → ignore or greet

---

### Conversation System

**Model:** Keyword listener — the NPC monitors nearby chat for trigger words. No dialogue trees, no clickable UI. Entirely text/chat driven. NPC responses are open chat (visible to all players in range).

**Keyword modes:**
- **Passive** — NPC always listening within a configured range tier, no hail required, no conversation lock. Used for passwords, magical wards, ambient triggers, custom conversation openers. Uses its own range/LOS check — independent of the targeting system (e.g., NPC behind a door can hear a password without being targetable).
- **Active** — only fires within an established conversation session (post-hail, locked to that player).

**Conversation state machine:**
```
Idle → InConversation(playerId) → Idle
              ↓ timeout
         OnConversationEnd (canned closing line) + player cooldown → Idle
```
- `Idle`: NPC hears passive keywords from any player in range
- `InConversation`: locked to one player; other players' active keywords ignored; indicator visible to nearby players
- `OnConversationEnd`: fires on timeout, closing keyword ("farewell"), or player leaving short range

**Hail:** a reserved passive keyword that triggers `OnConversationStart`. Extensible — any passive keyword can be designated as a conversation opener (e.g., a knock, a secret phrase, a custom greeting per NPC).

**Keyword matching flow:**
1. Player types in chat within NPC's configured range tier
2. NPC checks keyword mode: passive (always evaluates) or active (only if in conversation with this player)
3. If match: check faction requirement for that response
4. If faction requirement met: NPC delivers response, optionally applies faction hits, fires script events, unlocks additional active keywords for this player
5. Keyword progression is per-player and session-scoped

**Faction gating:** per-response requirement, not a gate on access. Player can always type — NPC just won't respond if standing is insufficient.

**Response markup:** response strings support substitution tokens resolved at delivery time from the triggering player's character data. Minimum token set: `<name>`, `<race>`, `<class>`, `<gender>`. Example: `"Well met, <class>!"` → `"Well met, Ranger!"`. Authoring happens in the conversation editor (future tooling); the runtime just performs token substitution before delivering the response.

**Quest model:** no explicit quest UI or quest-giving. Players infer objectives from dialogue and environmental clues.

**NPC-initiated speech:** NPCs can speak unprompted via:
- `OnPerceived` — greeting or hostile warning based on faction standing and range tier
- Time-based triggers (`IOnTimer`) — ambient dialogue on a configurable interval
- Any script event — any NPC event can trigger a say/emote response

**Keyword persistence:** unlocked keywords are session-scoped only — reset when the conversation ends. No server-side tracking of per-player keyword history.

---

### Enemy AI System

**Movement:** Unity NavMesh (NavMeshAgent) — required for obstacle avoidance. Scene must have navmesh baked.

**State machine:**
```
Idle/Wander → Perceive → Chase → Combat → (target dead) → Return → Idle/Wander
```

| State | Behavior |
|---|---|
| `Idle` | Random wander within a wander radius around spawn point |
| `Chase` | NavMesh path to target; re-evaluates each tick |
| `Combat` | In attack range — stop moving, auto-attack |
| `Return` | NavMesh path back to spawn point; heals/resets on arrival |

**Aggro entry — faction-driven (current):**
- `IOnPerceived` fires when a player enters perception range
- Resolve player's faction standing with this NPC's faction
- `KOS` or `Threatening` → enter Chase state
- `Dubious`/`Apprehensive` → warn only (no aggro)
- `Indifferent`+ → ignore

**Aggro entry — social (future):**
- When an NPC enters Combat, it broadcasts to nearby NPCs of the same faction
- Allies within social radius aggro the same target without a separate perception check

**Target reassessment (after kill):**
1. Enemy's current target dies → scan for nearest player within aggro range
2. If found → re-enter Chase with new target
3. If none → enter Return state, then resume Idle/Wander

**Wander (idle behavior — current):** pick a random NavMesh point within `wanderRadius` of spawn, walk there, pause, repeat. Wander replaced by waypoint patrol when `NpcWaypoints` component is present (future).

**Waypoint patrol (future):** `NpcWaypoints` component holds an ordered list of world-space transforms. Enemy walks the loop; wander is disabled while waypoints are active. Pause duration configurable per point.

**Key decisions:**
- NavMesh over CharacterController steering — obstacles in scene make raw steering unreliable
- State machine lives on server only (`[Server]` methods); position synced via `NetworkTransformReliable`
- Aggro type (faction vs. social) is a component flag, not a subclass — same `EnemyAI`, different config

---

### NPC Script Event System

**Model:** Interface-based components (Option A). Each behavior is a MonoBehaviour implementing one or more event interfaces. The NPC discovers them via `GetComponents` at runtime.

**Event interfaces (planned):**

| Event | Authority | Trigger |
|---|---|---|
| `IOnSpawned` | Server | NPC enters world |
| `IOnPerceived` | Server | Player enters perception range |
| `IOnTargeted` | Client | Player clicks to target this NPC |
| `IOnConversationKeyword` | Server | Matching keyword heard in chat |
| `IOnAttacked` | Server | NPC receives damage |
| `IOnFactionChanged` | Server | NPC's standing with a player changes |
| `IOnAggroLost` | Server | NPC loses its target (all threats cleared) |
| `IOnDeath` | Server | NPC health reaches zero |
| `IOnConversationStart` | Server | Player hails (or passive opener keyword matched) |
| `IOnConversationEnd` | Server | Conversation ends naturally, times out, or player leaves range |
| `IOnTimer` | Server | Periodic tick for ambient behavior |

**OnPerceived** is the faction system entry point — it resolves standing and routes to aggro, warning, or greeting behavior.

---

## Current Status
| Area | State |
|---|---|
| Player movement (walk/sprint/jump) | Working |
| Mouse look (RMB) | Working |
| Click-to-target + highlight | Working |
| Health system (SyncVar) | Working |
| NPC Event System | Working |
| Faction System | Working |
| Conversation System | Working |
| Enemy AI / movement | Working |
| Player autoattack (key 1 toggle) | Working |
| Enemy autoattack | Working |
| Aggro system | Placeholder only — needs design |
| Floating nameplates (mobs + players) | Working |
| Chat system | Working |
| Zones (3-zone MVP: transitions, interest isolation, per-zone spawn, persistence) | Working — flat scaffolds, content in 3.1 |
| Inventory (8 slots + PP/GP/SP/CP) | Working |
| UI (health bar, target frame) | Working |
| UI (inventory window) | Basic — needs polish |
| UI panel lock/unlock (context menu) | Not started |
| Win/lose conditions | Not started |

## Roadmap

> **Forward planning now lives in [`roadmap.md`](roadmap.md)** (numbered milestones/items, devplan-driven). Current milestone: **M1 — Identity & Persistence** (Postgres-in-Docker). **Workflow rule: every roadmap item gets a reviewed devplan in `docs/devplans/<id>-<slug>.md` BEFORE implementation.** The phased list below is retained as the history of what's been built; treat `roadmap.md` as authoritative for what's next.

### Phase 1 — Close the Core Loop
> Goal: fight, die, and progress. Everything else builds on this.

- [x] **Death handling** — player drops corpse with all items + copper; 10% XP loss (of next level's full cost); respawn at bind point (hardcoded to spawn position; `SetBindPoint()` wired for future spells/abilities); corpse owner-only lootable; full corpse looting recovers half the lost XP; de-leveling on XP loss supported
- [x] **XP + money on kill** — `MobKillReward` component implements `IOnDeath`; reads `xpReward` from `MobDefinition`; awards directly to attacker; Reward channel chat messages; level-up notification on XP award
- [x] **Level system** — 50-level XP table (1,000 → 10,291,400 per level); `PlayerExperience` uses static `XpTable[]` array; `ComputeLevel` / `TotalXpToReachLevel` / `XpForLevel` updated; `MaxLevel = 50`; level-up + de-level chat notifications; death XP loss = 10% of current level's span. `MobDefinition` gains `mobLevel` field (identity, shown in Mob Editor). Race + class XP modifiers: `RaceDefinition` and `ClassDefinition` ScriptableObjects (`Assets/Ueq/Race Definition`, `Assets/Ueq/Class Definition`) each carry a float `xpModifier`; `PlayerExperience` syncs combined `_xpModifier` SyncVar (race × class); all threshold math applies it; `SetRaceClass()` called at character creation (Phase 4). Inspector fields `_defaultRace`/`_defaultClass` on player prefab for pre-Phase-4 testing. Note: `RaceDefaultsTable` still uses string race names — migrate to `RaceDefinition` refs in Phase 4.

### Phase 2 — Make Characters Matter
> Goal: individual identity and meaningful gear choices.

- [x] **Character stats** — per-player STR/STA/AGI/DEX/INT/WIS/CHA; `CharacterStats` NetworkBehaviour (base from class, racial modifiers, equipment bonuses, SyncVar totals); `CombatStats` retired; Race & Class Editor exposes stat fields; `PlayerExperience.SetRaceClass` forwards to `CharacterStats.SetRaceClass`
- [x] **Hitpoint calculation** — `effectiveSta = min(Sta, classStaCap)` → `staModifier = classBaseStaRatio + (level-1) × classStaGrowthRate` → `maxHP = classBaseHP + (level-1) × classHpPerLevel + effectiveSta × staModifier`. Five new fields on `ClassDefinition`: `classBaseHP` (15/14/13/12), `hpPerLevel` (4/3/2/1), `staCap` (255/180/140/100), `baseStaRatio` (0.23 all), `staGrowthRate` (0.15/0.13/0.12/0.12) for melee/hybrid/healer/caster. Starting STA expected 55-105; level 1 HP lands 30-39 across archetypes; level 50 geared-to-cap: melee 2143, hybrid 1349, healer 966, caster 672. `Health.EffectiveMax` computes dynamically; `RefreshMax()` called on level-up and race/class change; falls back to flat `maxHealth` when no class assigned. **Pending editor setup**: configure ClassDefinition HP fields, assign `_defaultClass` on `PlayerExperience`
- [x] **Mana calculation** — `effectiveStat = min(INT or WIS, classManaCap)` → `manaModifier = classManaBaseRatio + (level-1) × classManaGrowthRate` → `maxMana = classBaseMana + (level-1) × classManaPerLevel + effectiveStat × manaModifier`. Mana stat per-class: INT → Wizard/Mage/Necro/Enchanter/SK/Bard; WIS → Cleric/Druid/Shaman/Paladin/Ranger; none → Warrior/Monk/Rogue (`ManaStatType.None` → max = 0). Five fields per class: `baseMana` (40/35/25), `manaPerLevel` (5/4/3), `manaCap` (200/200/150), `baseManaRatio` (0.23 all), `manaGrowthRate` (0.18/0.16/0.11) for caster/healer/hybrid. Level 1: 43-58 mana. Level 50 geared-to-cap: caster 2095, healer 1845, hybrid 1015. `PlayerMana` NetworkBehaviour with `UseMana`/`RestoreMana`/`RefreshMax`. **Pending editor setup**: configure ClassDefinition mana fields
- [x] **Combat calculation** — Weapons carry `baseDamage`, `delay`, and category (`Might` or `Finesse`). `ATK` is a derived stat: Might → `(STR×0.75)+(DEX×0.25)`; Finesse → `(DEX×0.75)+(STR×0.25)`. Per swing: (1) Dodge/Parry/Block — deferred (no skill system yet); (2) `hitRoll = clamp(rand(0,100) + (ATK-AGI) × shiftFactor, 0, 100)` maps to Miss (0-2.5) / Glancing (2.5-25) / Normal (25-75) / Solid (75-97.5) / Critical (97.5-100); (3) `rawDamage = weaponBase × (1 + relevantStat/400) × typeMultiplier × variance` — multipliers: Miss 0%, Glancing 50%±10%, Normal 100%±15%, Solid 150%±15%, Critical → Solid (skill untrained); (4) `finalDamage = rawDamage × (1 - AC/1000)` — AC=0 until equipment exists. `shiftFactor = 0.15`. Miss sends combat chat message instead of calling TakeDamage. Serialized weapon fields on `PlayerAutoAttack` stand in for equipment
- [ ] **Aggro / threat calculation** — numeric threat table per mob; threat generated by damage, heals, and abilities; formula deferred to grouping phase (threat only matters in group context). Current state: `EnemyAI` has threat list API (`AddThreat`) wired to damage, but formula is 1:1 damage = threat with no decay
- [x] **Hitpoint recovery** — `PlayerRegen` NetworkBehaviour; 1 HP/tick, 6s per-player server-side tick; only when alive and below max. Sitting (2/tick) designed, deferred until sitting state exists
- [x] **Mana recovery** — same `PlayerRegen` tick as HP; 1 mana/tick, 6s interval. Sitting (2/tick) deferred
- [x] **Equipment system** — equip slots (head, chest, legs, hands, feet, back, neck, 2× ring, 2× ear, weapon, offhand); item stats modify character stats via `CharacterStats.AddEquipmentBonus` / `RemoveEquipmentBonus`; equip/unequip from inventory
- [ ] **Item values** — `buyPrice`/`sellPrice` on `ItemDefinition`; weight (future encumbrance hook)
- [x] **Ability system** — data-driven, effect-composition model:
  - `AbilityDefinition` (ScriptableObject) — id, name, targeting type, range, cast time, cooldown, resource cost, ordered `AbilityEffect` list
  - `AbilityEffect` (abstract ScriptableObject) — one concrete subclass per mechanic: `DamageEffect`, `HealEffect`, `DotEffect`, `HotEffect`, `BuffEffect`, `DebuffEffect`, `SnareEffect`, `RootEffect`, `StunEffect`, `FactionHitEffect`; server calls `Apply()` on each in sequence
  - `PlayerAbilities` (NetworkBehaviour) — known ability list, hotbar slots, cooldown tracking, resource (mana/endurance/focus per class)
  - `AbilityRegistry` — loads `AbilityDefinition` assets from `Resources/Abilities/` at runtime
  - `HotbarUI` — key-mapped slots (distinct from autoattack); cast bar for timed casts
  - New ability = new ScriptableObject asset; new mechanic = new `AbilityEffect` subclass; no existing code touched
  - Warrior abilities are free (no mana cost); balance comes from a complex cooldown configuration that forces meaningful choices about which skill is available at any given time
- [ ] **Player grouping** — form/disband party (up to 6); group member health frames in UI; shared XP split evenly among members in kill range; friendly-fire rules (group members cannot damage each other by default); `MobKillReward` updated to distribute XP across group; `/invite`, `/disband`, `/leave` chat commands

### Phase 3 — Economy and Content Hooks
> Goal: give players things to work toward and spend currency on.

- [x] **Shop (buy/sell)** — `VendorApplicator` + `VendorInventory` + `VendorUI`; buy/sell tabs; wired into conversation system via `IOnConversationKeyword` ("wares" keyword); `CmdBuyItem`/`CmdSellItem` on `NetworkedPlayer`; `ItemDefinition` gains `buyPrice`/`sellPrice`
- [ ] **Quest reward trigger** — `IOnConversationKeyword` action that grants XP + items + faction hits; no quest UI — reward fires on correct keyword with faction gate
- [ ] **NPC item giving** — player trades item to NPC via conversation keyword; server validates inventory, removes item, fires script event

### Phase 4 — World and Identity
> Goal: the player is someone, and there is somewhere to go.

- [ ] **Player creation** — name, race, class selection screen; race seeds faction defaults; class sets base stats and grants starting `AbilityDefinition` list
- [ ] **Aggro system** — replace placeholder; faction-driven perception (already designed); social aggro (same-faction NPCs join combat within radius)
- [ ] **Second zone** — zone transition architecture (trigger volume → loading screen → new scene); proof of concept with one additional area; determines what state crosses zone boundaries

### Phase 5 — Persistence
> Goal: progress survives a server restart. Build last — schema should be stable by now.

- [ ] **Database persistence** — server-side storage for: XP, level, character stats, faction scores, inventory, currency, bind point; connection pooling; session auth token

### Phase 6a — Content Scale
> Goal: enough content to fill the progression curve. Runs in parallel with Phase 4–5.

- [ ] Fill mob roster, zone layouts, and item pool to cover level range
- [ ] NPC waypoint patrol — `NpcWaypoints` component; replaces wander when present
- [ ] Conversation editor — EditorWindow for authoring `ConversationKeywordSet` assets
- [ ] NPC editor — EditorWindow for NPC config (faction, keywords, waypoints, vendor stock)
- [ ] Balance pass — XP curve, drop rates, spawn density, shop prices against real play data

### Phase 6b — Infrastructure Scale
> Goal: real players on real hardware. Unknowable until load data exists — don't over-engineer early.

- [ ] Load testing — find actual player ceiling per zone server
- [ ] Zone server architecture — one process per zone or shared; inter-zone communication
- [ ] Authentication + accounts — login, session tokens, transport-layer anti-cheat
- [ ] Persistence at scale — write throughput, cache strategy, connection pooling under load

### Phase 6c — Client Distribution
> Goal: players can connect without Unity installed.

- [ ] Build pipeline — automated client builds per platform
- [ ] Launcher / patcher — version check on launch, delta patching
- [ ] Update delivery — push client updates without full re-download

---

## Deferred / Backlog
Items that are designed but not yet scheduled into a phase:

- **Inventory UI polish** — slot icons, borders, tooltip on hover, equip/use/drop interactions
- **Right-click context menu** — `UIPanelContextMenu` on all moveable panels; Lock/Unlock position+size
- **Chat extensions** — `/say`, `/shout` (wider range), `/whisper <name>` already implemented; `/group`, `/yell` future
- **Item pickup** — no pickup mechanic yet; loot window covers corpse looting
- `GameNetworkManager.SpawnEnemy` / `DespawnEnemy` server hooks

## Key Decisions
- **Cursor free by default** — RMB to look so LMB stays available for targeting
- **CharacterController** over Rigidbody for predictable networked movement
- `NetworkTransformReliable`: **players sync position + rotation (yaw)** — `NetworkedPlayer.Awake` forces `nt.syncRotation = true` so remote players visibly turn (the body transform is yaw-only; camera pitch is separate + local-only, and the local player's NT is disabled on clients so there's no camera-lag cost). Mobs still sync position only (rotation sync for mobs is a 3.0.1 open item). *(Was "position only, rotation client-side" — that only ever suited the local player and left remote players frozen-facing; fixed in 3.1 once multi-client testing surfaced it.)*
- **Damage sources** are all unified through `Health.TakeDamage` (abilities, spells, environment, auto-attack)
- **NPC events** are interface-based (`IOnSpawned`, `IOnPerceived`, etc.) discovered via `GetComponents` on `NpcEventDispatcher`
- **Zone architecture: single-server, multi-scene** (proven by spike 2.0, ✅ GO). A zone = an additively-loaded scene with `SceneInterestManagement` partitioning by scene + per-scene physics. Server-per-zone (EQ-style world+zone-server split) is the same abstraction at a larger deployment size, so it's **not a fork** — it's **deferred to M6b** and gated on load-test data, not intuition. To keep the future split cheap, treat **zone as first-class**: `zoneId` on entities + persisted character state, chat behind a per-zone delivery interface, persistence keyed so a zone could move. Transition rules (from the spike): server-authoritative movement, teleport via `NetworkTransform.ServerTeleport` (never a raw transform set), no client-side physics body on players, portals via server-side proximity (not physics triggers). Full detail in `docs/devplans/2.0-zone-architecture-spike.md`.

## Last Session
<!-- Updated automatically at end of each session -->

> **▶ RESUME HERE (next session) — 3.1.4 ✅ DONE. 3.1.5 IMPLEMENTED (code only). USER: verify in-editor (set spell_casting.fbx→Humanoid → Build Player Locomotion Controller → Build Class Content → test Wizard/Cleric/Warrior abilities), then mark ✅ → 3.1.6 (char-select redesign + create-form UX fix + weapon props). Open: user mulling a deferred "polish pass" bucket. See 2026-07-05 entry.**
> - **3.1.1 + 3.1.2 are DONE + verified in-editor** (3.1.1 committed; 3.1.2 uncommitted at session end — user commits). **3.1.2 delivered:** left-anchored menu over the real cover-fit art (`Assets/Resources/UI/Title/TitleBackground.png`) + left scrim, stylized **fantasy-font** TMP "UEQ" wordmark (drop-in font at `Resources/UI/Title/TitleFont`, warm-gold gradient + soft shadow), Play accent button + hover/press states, dev/Host tucked bottom-left, version label. **AND (approach A) folded Login/Register/Connecting into inline modes of the title** — pre-connect + connecting flow stays on the art with **no black transition**; fade reserved for auth-success → Character Select. `ClientScreen` → `{Title, CharacterSelect, InWorld}`; `LoginPanel`/`RegisterPanel`/`ConnectingPanel` removed; `TitlePanel` owns the Menu/Login/Register mode machine + connecting busy-state. User verified: menu → Play (inline) → wrong pw inline error → correct → fade to Character Select → logout → clean title. **Commit 3.1.2 when ready.**
> - **3.1.2 font DONE:** fantasy wordmark font loaded via the drop-in hook (TMP Font Asset at `Resources/UI/Title/TitleFont`; user imported a TTF → Font Asset Creator → saved to that path). Wordmark now stylized.
> - **Inline login IMPLEMENTED (approach A, code — pending in-editor verification):** user approved folding **Login + Register + Connecting into inline modes of `TitlePanel`** so the whole pre-connect + connecting flow stays on the title art with **no black-screen transition**; the shell fade is now reserved only for auth-success → Character Select (and logout back to Title). Changes: `ClientScreen` dropped Login/Register/Connecting → `{Title, CharacterSelect, InWorld}`; `UIScreenManager.ComputeTarget` returns Title for disconnected **or** connecting-not-yet-authenticated; removed `GoTo`/`_preConnect`; `TitlePanel` now owns a `Menu/Login/Register` mode machine (rebuilds the left column's form area under the persistent wordmark) + a connecting busy-state (status line "Connecting…", inputs/buttons disabled while `NetworkClient.active`, Esc cancels); removed `LoginPanel`/`RegisterPanel`/`ConnectingPanel`. Same Mirror/auth glue + Tab/Enter/Esc nav carry over. Grep-verified no dangling refs. **NEXT (user, in editor): recompile → Play → Title art shows menu; Play → login fields appear inline (no fade); wrong password → "Invalid…" inline (no black flash); correct → fades to Character Select; camp/logout → fades back to a clean title menu. Then commit.**
> - **3.1.3 DONE + verified in-editor 2026-07-04** (uncommitted at session end — user commits): styled the inline title login/register form — `MenuUI.RoundedSprite()` generated 9-slice on all buttons + fields (fixed a latent double-tint: graphic white, ColorBlock tints), input focus-highlight (L5), "Sign In"/"Create Account" heading (L3), primary Log In + lighter Secondary buttons (L4), Server field moved to the dev cluster (L6 — player login = username/password only, default localhost). Character Select inherits the rounded style. Devplan `docs/devplans/3.1.3-login-form-styling.md`.
> - **NEXT: the client-UX track of 3.1 (shell/title/login) is DONE (3.1.1–3.1.3).** Remaining onboarding work is **Track B (art/content/Unity-editor: asset imports, prefabs, ScriptableObjects)**, not code.
> - **Synty asset strategy (decided this session):** user owns **Sword-Combat anims, Fantasy Knights (modular knights), POLYGON Fantasy Rivals (varied fantasy chars), Prototype, Dungeon bundle**; already imported = PolygonAdventure + PolygonGeneric (~185 MB). **No elf / clean 2nd-race model in any pack** → **DESCOPE the 2nd race**: 3.1.4 becomes **Human-only, race system built to accept a 2nd race later** (defer Elf to a future Synty sale). Visual variety at char-create now comes from **class, not race** — **3.1.5 carries it via Fantasy Rivals models** (distinct Warrior/Wizard/Cleric). Agreed approach: **don't bulk-import-then-purge** (Unity purging is unreliable + commits bloat private-repo history forever); instead **inventory + import per-vertical + gitignore-while-exploring** (adopt a pack only when used). Import **Fantasy Rivals** (+ Fantasy Knights) for the class models; Prototype/Dungeon/Sword-Combat wait for their verticals (3.1.8/3.6/M4).
> - **Fantasy Rivals IMPORTED** at `Assets/Synty/PolygonFantasyRivals` (Humanoid rig, `animationType:3` → retargets existing Mixamo locomotion). Added to `.gitignore` (gitignore-while-exploring; un-ignore on adoption). **Roster reframes the plan:** it's an enemies/bosses pack (Troll, BigOrk, RedDemon, 3 Golems, Medusa, SpiritDemon, MutantGuy, PigButcher, Slayer, etc. → a **mob goldmine for M3/M4**), BUT it also contains a **`Dwarf` and `DarkElf`** (both Humanoid) → **2nd race is viable after all** (recommend **Human [PolygonAdventure] + Dwarf [Rivals]** instead of descoping; DarkElf = antagonist-styled alt; or still hold for a proper Elf). **Class models:** Warrior = PolygonAdventure Knight/Warrior (heroic, in-project); Wizard/Cleric best-effort from Rivals `Mystic`/`ForestWitch`/`ForestGuardian` but villain-styled — **user to eyeball prefabs (Dwarf, DarkElf, Mystic, ForestWitch, ForestGuardian, AncientWarrior) for hero-vs-villain read** since filenames alone don't tell. **DECIDED:** race #2 = **Dwarf** → lineup **Human (PolygonAdventure) + Dwarf (Fantasy Rivals)**, both Humanoid, race system extensible for a real Elf later. **Class models: Rivals options rejected as villain-styled — user is shopping for a cheap class-appropriate pack.** Plan: **do 3.1.4 (races) NOW (unblocked)**; **split 3.1.5** → class **data** (stats + 1 ability each: Warrior Kick exists, Wizard nuke, Cleric heal — unblocked, ScriptableObjects) vs class **models** (blocked on the pack). Flagged the cheaper alt to the user: **class shows via gear (weapon/robe on the race body), not a separate body model** — may avoid needing a class pack at all. Char-select preview driven by **race** for now; class flavor via a starting weapon prop is a cheap interim. PolygonAdventure chars in project: Knight/Warrior/Viking/Peasant/Shopkeeper (+color variants). Do NOT `git add` PolygonFantasyRivals until adopted.
> - **Class-art plan (SETTLED pending purchase):** user eyeing **POLYGON Fantasy Characters Pack** (~$10 w/ Humble coupon). Reviewed its roster (12 **pre-made** chars — NOT modular, so simple single-prefab use — + caster props): **Male Bard/King/Peasant/Rogue/Sorcerer/Wizard, Female Druid/Gypsy/Peasant×2/Queen/Witch**; props incl. Wizard/Druid Staff, Sceptre, Crystal Ball, Spellbooks, Ornate Sword, Dagger, Wand. **It's caster/civilian-heavy: solves Wizard (Male Wizard) + Cleric (Female Druid or Male Sorcerer) — the exact gap Rivals couldn't — but has NO Warrior/Knight and NO Elf.** Those gaps are already covered: **Warrior = PolygonAdventure Knight/Warrior (owned)**; **Elf still unavailable → race #2 stays Dwarf (Rivals).** Claude recommends the buy (fills the caster hole; consistent Synty art). **Final onboarding model lineup:** Race Human = PolygonAdventure; Race Dwarf = Fantasy Rivals; **Warrior** = PolygonAdventure Knight/Warrior; **Wizard** = Fantasy Characters Male Wizard; **Cleric** = Fantasy Characters Female Druid (or Male Sorcerer). Props = class-via-gear flavor (give Wizard a staff, etc.). **If bought: user tells Claude the landed folder → Claude gitignores it → draft the 3.1.4 (races: Human+Dwarf) devplan, then 3.1.5 (classes: the 3 models above + 1 ability each).**
> - **Customization Q&A + logged roadmap 4.8:** user asked (before committing to Synty) about eventual EQ-level customization — visible weapon/armor swap + unique faces. Answer: **weapon-swap works on ANY Synty character now** (attach mesh to hand socket, rig-independent); **armor-piece + face swapping needs MODULAR characters** — the pre-made packs (Fantasy Characters/PolygonAdventure/Rivals) are single-mesh and can't do it. The modular path = **Synty Sidekick** (user already owns **Fantasy Knights – Sidekick**). Pre-made packs don't block/enable modular (orthogonal) and stay useful as NPCs/mobs. Logged **roadmap 4.8 — Character appearance & visible equipment (modular/Sidekick)**: classic-EQ target (faces/hair at creation, visible weapons, visible armor tiers), players migrate to Sidekick, weapon-swap = cheap first slice. So the $10 Fantasy Characters buy is fine for the 3.1 onboarding look; deep customization is a deliberate later Sidekick track.
> - **Art-pipeline guidance (DIY vs buy):** user targets **EQ1-level art** and asked whether to make their own vs use Synty as stand-in. Guidance given: **art isn't this game's bottleneck (systems/content are — EQ lived on mechanics, not art); Synty already exceeds EQ1's look.** By category: **characters = BUY (Synty)** — hardest 3D discipline (modeling/topology/faces = months); the Mixamo/Humanoid pipeline already solves the worst parts (rig+anim), so DIY chars = wrong time-sink for a worse-than-Synty result at the EQ1 bar; keep Synty for characters indefinitely. **Props/weapons = great DIY entry point** (learnable in weeks; you'll want custom item variety anyway; MATCH Synty's flat-shaded texture-less low-poly look — easier than EQ1 hand-painted). **Environments = middle** (modular kits learnable, or use owned Synty Prototype/Dungeon packs). Don't mix DIY+Synty *within* a category (consistency); style-matched DIY props are fine next to Synty chars. **Decision: user grabbing the Fantasy Characters pack now; keep momentum on systems/content; DIY art optional on props later.**
> - After Track B: **3.1.6** char select/create redesign + live 3D preview (converges Track A+B), **3.1.7** camp/logout polish, **3.1.8** light starting-location polish (Prototype pack). Each sub-item gets its own devplan first.

- 2026-07-05: **POLYGON Fantasy Characters pack IMPORTED + adopted into the onboarding art plan; Track B (3.1.4/3.1.5) now fully unblocked.** Landed at `Assets/Synty/PolygonFantasyCharacters/` — **gitignored** (gitignore-while-exploring; do NOT `git add` until adopted). Verified `Models/Characters.fbx` is `animationType: 3` (Humanoid, single shared avatar) → existing Mixamo locomotion + Kick retarget with no new anim work, same as PolygonAdventure/Rivals. Roster confirmed: Male Wizard/Sorcerer/King/Baird/Rouge/Peasant, Female Druid/Gypsy/Queen/Witch/Peasant×2 + caster props (Wizard/Druid Staff, Crystal Ball, Wand, Sceptre, Spellbooks, Ornate Sword, Dagger). **Decisions this session:** (1) sequencing = **3.1.4 races first**, then 3.1.5 classes; (2) **Cleric class model = `SM_Chr_Female_Peasant_02`** (user's call — plain-commoner "novice acolyte" read; separates it from the robed Wizard; give her a Sceptre/Spellbook prop). Final lineup: Race Human = PolygonAdventure · Race Dwarf = Fantasy Rivals · Warrior = PolygonAdventure Knight/Warrior · Wizard = `SM_Chr_Male_Wizard_01` · Cleric = `SM_Chr_Female_Peasant_02`. **Grounded the code for the 3.1.4 devplan:** `RaceDefinition` (SO, `Assets/Ueq/Race Definition`, in `Resources/Races`) currently has NO model ref — just name/xpMod/statMods; `RaceClassRegistry` loads races/classes from `Resources/{Races,Classes}` by name; race is already synced client-side via `PlayerFactionScores.ActualRace` `[SyncVar]`; char-create race/class come from `CharacterSelectController` → `RaceClassRegistry.AllRaceNames()/AllClassNames()` → `CharacterListMessage.raceOptions/classOptions` → `CharacterSelectPanel` cycle buttons (no preview yet; live 3D preview is 3.1.6); the Player prefab hard-parents ONE Synty child model (Knight) with Animator + `PlayerAnimator` — needs to become race/class-driven. **DESIGN SETTLED + 3.1.4 DEVPLAN DRAFTED** (`docs/devplans/3.1.4-race-models.md`, 🟡 awaiting review, RM1–RM7). Resolved the race-vs-class body contradiction with a better model: **gender is picked FIRST and cascades** (gender → available races → available classes); **race drives the body mesh, class drives a (future) weapon prop.** Every legal combo maps to a model we own — the matrix falls out of the assets: **Male Human** = Warrior (`SM_Chr_Warrior`) / Wizard (`SM_Chr_Male_Wizard_01`); **Male Dwarf** = Warrior / Cleric (both use the one Rivals `Dwarf` body); **Female Human** = Warrior (`SM_Chr_Female_Gypsy_01`) / Wizard (`SM_Chr_Female_Peasant_01`) / Cleric (`SM_Chr_Female_Peasant_02`). No male-human-Cleric (only a female cleric body), no female Dwarf (Rivals dwarf is male) — both fall out naturally. Resolution = a small **(gender,race,class)→prefab** table, dwarf collapsing its 2 classes to one body; all packs Humanoid so Mixamo locomotion/Kick retarget with no anim work. **NEW SCOPE the devplan folds in: gender is a brand-new persisted attribute** (never existed; only the `<gender>` conversation token reserved it) → adds a `Gender` enum, migration `0018`, message/repo plumbing, and finally lets the `<gender>` token resolve. Devplan decisions: RM1 one authored `CharacterRoster` SO in Resources (= create-form options + server whitelist + client model map; a flat per-race field can't express class-avail-by-(gender,race)); RM2 persist gender (0018, default Male); RM3 sync (gender,race,class) to the client (race already syncs via `PlayerFactionScores.ActualRace`); RM4 new `PlayerModel` runtime-instantiates the full `SM_Chr_*` prefab under a ModelRoot (full prefab, not bare mesh — the T-pose-saga lesson) + rebinds `PlayerAnimator`/`PlayerLocomotion`; RM5 char-create Gender→Race→Class cycle cascade filtered from the roster (styled picker + 3D preview stay 3.1.6; keep legacy IMGUI `CharacterSelectUI` compiling); RM6 weapon props OUT → fold into 3.1.5; RM7 author Human+Dwarf `RaceDefinition` + the roster asset. Build staged: data/migration → creation cascade → in-world model swap (verify each of the 7 tuples on a remote MPPM client) → regression. Grounded in real code (`CharacterSelectController`, `CharacterSelectMessages`, `RaceDefinition`, `RaceClassRegistry`, `CharacterSelectPanel`). **User approved RM1–RM7 (all as recommended) → IMPLEMENTED all 3 stages (code only, pending in-editor verification).** New files: `Gender.cs` (plain enum), `CharacterRoster.cs` (SO + `RosterEntry`), `CharacterRosterRegistry.cs` (Resources-loaded; Genders/RacesFor/ClassesFor/IsValid/GetModel/AllOptions), `PlayerModel.cs` (client-only MonoBehaviour — polls synced (gender,race,class), instantiates the roster body under an auto-created `ModelRoot`, wires locomotion controller + `PlayerAnimator` before AddComponent to dodge the T-pose), `0018_character_gender.sql` (`gender TEXT NOT NULL DEFAULT 'Male'`), `Editor/CharacterRosterSetup.cs` (`Tools/Character/Build Character Roster` — creates Dwarf race + Wizard/Cleric placeholder classes + the 7-tuple roster asset with Synty prefabs by path). Edits: gender plumbed through `CharacterSnapshot`/`PendingCharacter`/`CharacterRepository` (CreateIdentity gains gender param; Upsert/Load/CharacterRow/ListByAccount; tolerant `ParseGender`)/`CharacterPersistence`; `PlayerExperience` gained `[SyncVar] Gender _gender` + `[SyncVar] string _className` + `SetGender` (race stays on `PlayerFactionScores.ActualRace`); `CharacterSelectMessages` (+`gender`, +`CreateOption`/`createOptions`, kept legacy raceOptions/classOptions so IMGUI `CharacterSelectUI` still compiles); `CharacterSelectController` (sends createOptions; OnCreate validates the tuple via `CharacterRosterRegistry.IsValid`); `CharacterSelectPanel.BuildCreate` (Gender→Race→Class cascade); `NpcConversation` (`<class>`/`<gender>` tokens now resolve — were placeholders); `PlayerAbilities`/`PlayerAutoAttack` Rpcs lazily re-resolve `_animator` (model child rebuilt post-cache); `SceneSetup` patch tool adds PlayerModel + wires PlayerLocomotion. **NEXT (user, in editor): recompile → `Tools/Character/Build Character Roster` → `Tools/Database/Run Migrations` (0018; existing chars default Male) → DELETE the static Synty child from `Player.prefab` + `Tools/Patch Player Prefab` (adds PlayerModel, wires controller) → Play: gated Gender→Race→Class cascade (no male-human Cleric, no female Dwarf); each of the 7 tuples spawns the right body + animates (verify on a remote MPPM client); gender persists + relog reconstructs; NPC `<gender>`/`<class>` resolves. Then mark 3.1.4 ✅ + 3.1.5 (class stats/HP/mana finalize the Wizard/Cleric placeholders + 1 signature ability each: Kick exists, Wizard nuke, Cleric heal + weapon props axe/mace/staff).** Git: these packs are now adopted → un-ignore PolygonFantasyCharacters/Rivals so the roster's prefab refs commit cleanly. All uncommitted — user commits. **(cont.) — 3.1.4 VERIFIED + CLOSED 2026-07-05.** User confirmed in-editor ("it all works"): female-human Cleric (`SM_Chr_Female_Peasant_02`) created + logged in with the correct body + animation (proves roster lookup, runtime `PlayerModel` swap, Mixamo retarget onto a new pack's avatar, and the synced (gender,race,class) end-to-end). Marked roadmap **3.1.4 [x]** + devplan ✅. **User flagged the create form as rough** — triaged: cycle-buttons (vs dropdowns/arrow selectors, esp. gender) + the whole-form flicker on every click are the deliberate minimal UI (RM5/SF6) → **explicitly folded into 3.1.6** (redesign + live 3D preview; roadmap 3.1.6 updated to name these). But the **name-field-wiped-on-click** was a real bug (the per-click `Rebuild()` destroyed the name `TMP_InputField`) → **fixed now** in `CharacterSelectPanel` (capture `_pendingName` before teardown, null the stale ref, restore in `BuildCreate`). **NEXT: 3.1.5 — finalize the Wizard/Cleric placeholder ClassDefinition stats/HP/mana + 1 signature ability each (Kick exists; Wizard nuke; Cleric heal) + attach the class weapon prop (axe/mace/staff, deferred from RM6). Devplan-first.** All uncommitted — user commits (incl. un-ignoring the adopted packs). **(cont.) — DRAFTED the 3.1.5 devplan** (`docs/devplans/3.1.5-class-setup.md`, 🟡 awaiting review, CS1–CS7). Grounded in the real ability system — which already supports everything (no new mechanic): `AbilityDefinition` + `DamageEffect`/`HealEffect` (Str…Cha `ScalingStatType`), `AbilityTargetType {Self, SingleTarget}`, `PlayerAbilities.TryCast` (validates known/cooldown/range[skipped for Self]/mana, consumes mana, applies effects, fires optional `animTrigger`, GCD), `SetRaceClass` auto-fills the hotbar from `cls.startingAbilities`. Existing: `Kick.asset` (anim-only, effects empty) + `Taunt.asset`; Warrior already has Kick+Taunt; Wizard/Cleric are the 3.1.4 placeholders with no abilities (HP/mana already match the locked model). Decisions: CS1 confirm formulas + tune per-class base-stat spread; CS2 Wizard nuke (DamageEffect, Int scaling, SingleTarget r20, ~10 mana) + Cleric heal (HealEffect, Wis scaling, **Self**) + give **Kick a DamageEffect** (Str, free); CS3 one signature on each hotbar; CS4 **self-heal only** (friendly/group → M4); CS5 **no cast anim for MVP** (empty animTrigger; a "Cast" clip via the existing `AddTriggeredState` pattern is a cheap follow-up); CS6 weapon props via `Animator.GetBoneTransform(HumanBodyBones.RightHand)` (rig-independent) in `PlayerModel` — wire **Warrior `SM_Wep_Sword_01` + Wizard `SM_Prop_WizardStaff_01` now; Cleric mace DEFERRED (no mace in any pack)**; **first step: verify the Synty bodies don't already ship a held weapon** (some premades do → don't double up); final grip alignment is far easier in 3.1.6's live preview; CS7 prop data on `ClassDefinition` (weaponPropPrefab + grip offset; class name already synced); CS8 extend the roster/class builder to stamp abilities+assignments one-click. Build staged: (1) class data + abilities (commit-able alone) → (2) weapon props. **(cont.) — 3.1.5 devplan APPROVED + tightened.** User resolutions: CS1–CS4/CS8 as recommended; **CS5 → wire a real Mixamo Cast clip** (user fetches it; extend `PlayerAnimatorSetup` to build a shared "Cast" trigger state via the existing `AddTriggeredState` pattern; both spells `animTrigger="Cast"`) — not "no anim"; **CS6/CS7 weapon props DEFERRED entirely to 3.1.6** (Cleric prop = `SM_Prop_Sceptre_01`, Warrior `SM_Wep_Sword_01`, Wizard `SM_Prop_WizardStaff_01`, grip-aligned in 3.1.6's live preview). So **3.1.5 = class data + abilities + cast anim only.** Roadmap 3.1.5/3.1.6 updated (props moved to 3.1.6 alongside the create-form UX fix). **Open process question the user raised:** whether to add a dedicated **polish-pass bucket** (props/animations/refinement itemized list) — Claude recommended NOT a near-term milestone (per the user's own "systems/content are the bottleneck, not art" call; polish is most valuable late once the roster is stable, ~Phase 6a; overlaps logged 4.8) but rather a deferred backlog bucket seeded with the deferrals (Cleric mace, extra cast/ability anims, weapon-prop grip offsets, weapon-swap); **awaiting user's yes/no to add it.** **NEXT: implement 3.1.5.** **(cont.) — 3.1.5 IMPLEMENTED (code only, pending in-editor verify).** User added `spell_casting.fbx` to `Assets/Animations/Locomotion` (⚠️ imports Generic by default → user must set **Rig=Humanoid + Loop off**). Extended `PlayerAnimatorSetup` to match a `cast`/`spell` clip → a **"Cast"** one-shot trigger state (same `AddTriggeredState` path as Attack/Kick). New `Editor/ClassContentSetup.cs` → `Tools/Character/Build Class Content`: creates **Fire Bolt** (`fire_bolt`, SingleTarget r20, 10 mana, `animTrigger="Cast"`, DamageEffect 12+Int×0.5) + **Minor Heal** (`minor_heal`, Self, 10 mana, Cast, HealEffect 20+Wis×0.5) as Resources/Abilities assets with the effect embedded as a **sub-asset**; gives **Kick** a DamageEffect (8+Str×0.5, free); assigns `startingAbilities` (Wizard=[FireBolt], Cleric=[MinorHeal], Warrior keeps Kick+Taunt). Idempotent. **No engine changes** — `PlayerAbilities`/effects already supported it. **Base stats left to the Race & Class Editor (CS1)** — avoids clobbering Warrior's Phase-2 tuning; Wizard/Cleric already have INT/WIS identity from the 3.1.4 placeholders. **NEXT (user, in editor): `spell_casting.fbx`→Humanoid+Loop off → `Tools/Build Player Locomotion Controller` (log shows `cast:…`) → `Tools/Character/Build Class Content` → Play: Wizard Fire Bolt (key 2, target mob → damage + Cast anim + mana), Cleric Minor Heal (self, after taking damage), Warrior Kick now damages; verify on a remote MPPM client. Then mark 3.1.5 ✅ → 3.1.6.** All uncommitted — user commits. Still open: the deferred "polish pass" bucket (awaiting user yes/no). **(cont.) — two bugs surfaced during 3.1.5 in-editor testing (fresh Wizard from editor renders + animates perfectly incl. the new Cast; an MPPM client on an OLD test-account fighter looked wrong):** (1) **remote-player rotation not synced** — the host wizard animated on the client but never turned. Root cause = the documented "rotation client-side" decision: player NT had `syncRotation` off, server sets yaw in `CmdSendInput` but it never reached remote clients. **FIXED:** `NetworkedPlayer.Awake` now forces `nt.syncRotation = true` (body transform is yaw-only; local player's NT is disabled on clients + host renders its own transform → zero camera-lag cost; overrides the prefab's serialized `syncRotation:0`). Updated the Key Decision. Mob rotation sync remains a separate 3.0.1 item. (2) **old test-account fighter renders as "a capsule with static arms sticking out"** (vs the fresh wizard which is correct) — UNRESOLVED, needs diagnosis. Hypotheses: the pre-3.1.4 character's stored (gender,race,class) doesn't resolve a roster model (→ bare capsule) OR the body builds but T-poses (no controller). Discriminating test given to user: **create a fresh Male/Human/Warrior via the cascade on the MPPM client** — if it renders like the wizard, old test chars just predate the roster/gender pipeline and need recreating (benign); if the fresh warrior is ALSO broken from the host, it's a genuine remote-build bug in `PlayerModel`. Asked user for: the host Console `[PlayerModel] No body model for …` warning (prints the exact tuple), the old fighter's race/class, and whether it's broken on its own client view too. **NEXT: user runs the fresh-warrior test + reports those, then re-tests rotation (now fixed) + the 3.1.5 abilities; then mark 3.1.5 ✅.**
- 2026-07-04: **Implemented 3.0 Stage C (world integration + persistence) — code only; partial in-editor verification.** Stage B was confirmed done first (isolation + transition + no-sink all passed 2026-07-03); logged two known quirks to new roadmap item **3.0.1** (rotation not synced — `Player.prefab` `syncRotation:0`, intentional; MPPM keyboard-focus + "2 event systems" warning — benign, no fix). **Stage C changes (all in `docs/devplans/3.0-zone-integration.md` Stage C log):** key insight = only `Instantiate`-created objects land in the wrong (active/base) scene; the mob `Corpse` is a component reused **on the mob GO itself** so it's already in-scene (no fix). (1) **`SpawnPoint.SpawnOne`** — `MoveGameObjectToScene(go, gameObject.scene)` before `NetworkServer.Spawn` so a zone's spawn point keeps its mobs in that zone. (2) **`PlayerCorpse`** (`NetworkedPlayer.HandlePlayerDeath`) — same move onto the corpse prefab. (3) **Death respawn zone-aware** — routed through new `ZoneManager.ServerPlaceInZone`; non-starter-zone death respawns at that zone's `default` entry (cross-zone bind not tracked → 3.0.1). (4) **Login into persisted zone** — `CharacterPersistence.ApplySnapshot` routes placement through `ZoneManager.ServerPlaceInZone(conn, zoneId, savedPos, yaw)` (scene move + client `SceneMessage` additive load + `ServerWarpTo` to saved world pos; unknown/removed zone → starter default entry); new chars `SetZone(StarterZoneId)`. (5) **`ZoneSetup.BuildThornwoodScene`** now drops a `Giant Rat` `SpawnPoint` in thornwood so per-zone spawn isolation is testable. Prepped `docs/m1-regression-checklist.md` for the zone-aware state (migrations 0001–**0017**, zone setup gates, reworded the EventSystem gate as the benign multi-client quirk, added inline zone assertions + a new **§9 Zone integration** section). **User ran Stage C verify: zoning + cross-zone mob isolation ✅; BUT thornwood rats spawn FROZEN** (don't wander/can't engage). **Diagnosed:** `WanderBehavior`/`EnemyAI` no-op when `NavMeshAgent.isOnNavMesh` is false → thornwood's navmesh isn't live at the +5000 offset at runtime. Root cause = `ZoneSetup`'s scripted `NavMeshSurface.BuildNavMesh()` reflection call doesn't persist the NavMeshData asset, so the additive scene loads with no navmesh. Logged to **3.0.1** (additive-zone navmesh). Keyboard-dead-on-both-clients = the known MPPM focus quirk (click into a client's Game view). **NEXT (user, in editor): hand-bake thornwood's navmesh — open `Assets/Scenes/Zones/thornwood.unity` → select `ZoneRoot` → NavMeshSurface **Bake** → confirm blue navmesh over the plane → SAVE scene → re-host → rat should wander (click into the client window for keyboard/auto-attack). If rats stay frozen with a visible blue navmesh, it's the deeper offset-navmesh sub-risk (runtime `AddNavMeshData` across additive scenes) → tell me and I'll dig in. If the manual bake fixes it, I can harden `ZoneSetup` to save the NavMeshData asset so re-runs don't need a hand-bake. Then finish Stage C checks (corpse-into-zone, per-zone death respawn, login-into-persisted-zone) + run the full zone-aware M1 regression.** **(cont.) — found the REAL cause of "frozen mobs": the Enemy prefab has NO NetworkTransform.** After the manual thornwood bake, testing as **Server Only + 2 remote MPPM clients** exposed it: mobs' server-side AI/navmesh work fine (the thornwood rat chased, caught up, and attacked; player attacks read "out of range" because the server measures to the mob's real *moved* position), but the mob's *visual* never moves on remote clients. Root cause: `SceneSetup.CreateEnemy()` + `PatchEnemyPrefab()` add every networked component **except a NetworkTransform** (the player gets one; the enemy never did) — so mob movement was never network-synced. Masked for months because all prior testing was **Host** (host renders the server transform directly; no sync needed); the zone verification's Server-Only + remote-client setup is what surfaced it. NOT a navmesh or zone bug. **Fix (code, pending in-editor apply):** new `SceneSetup.AddEnemyNetworkTransform()` adds a server-authoritative `NetworkTransformReliable` (syncPosition on, syncRotation off, `coordinateSpace = World` for offset zones) — wired into both `CreateEnemy()` and `PatchEnemyPrefab()`; `EnemyAI.Awake` now disables the NavMeshAgent (prefab ships it `m_Enabled: 1`) so it only drives the transform on the server and doesn't fight the NetworkTransform on clients (`OnStartServer` re-enables it). Logged both to roadmap **3.0.1** (the NetworkTransform fix + the separate ZoneSetup navmesh-persistence tooling gap). **NEXT (user, in editor): recompile → `Tools/Patch Enemy Prefab` → re-save `Assets/Prefabs/Enemy.prefab` (it gains the NetworkTransform) → re-test Server Only + 2 MPPM clients: mobs should now visibly wander/chase on both clients, and attacks should register in range. Then finish the Stage C checks + zone-aware M1 regression.** Watch for client-side jitter (would mean the NavMeshAgent is still fighting the NT — tell me). **(cont. 2) — NetworkTransform fix CONFIRMED working:** mobs now sync + are killable in both zones (creslins rat roams). Two follow-ups: (1) **thornwood rat still not wandering** — diagnosed as thornwood's navmesh not live at runtime (mob spawns off-navmesh → `isOnNavMesh` false → wander no-ops); root cause = `ZoneSetup`'s scripted `BuildNavMesh()` doesn't persist the NavMeshData asset AND re-running the tool rebuilds thornwood from scratch (wiping any manual bake). **Fixed:** `ZoneSetup.BuildThornwoodScene` now saves the NavMeshData to `Assets/Scenes/Zones/thornwood_navmesh.asset` + keeps the surface pointed at it (reloads with the scene). Also debunked the earlier "creslins froze when thornwood baked" ping-pong — that was the missing-NetworkTransform bug, NOT a navmesh conflict; the two offset zone navmeshes coexist fine. (2) **mobs no longer aggro on proximity** (creslins rat roams but isn't hostile) — NOT from this session's changes; `NpcFaction.OnPerceived` aggros only when the player's Vermin standing ≤ Threatening, so it's faction data (character race's Vermin default, or `PlayerFactionScores`/Giant-Rat faction not resolving post-2.6-rekey). Logged both to 3.0.1. **NEXT (user, in editor): re-run `Tools/Zones/Build Zone Scenes` (thornwood navmesh now persists) → save SampleScene → re-test: thornwood rat should wander. For aggro, walk near the rat and read the server console `[Faction] … is <standing> — aggro/would warn/would greet` line + tell me the standing (+ the test character's race). Then finish Stage C checks + zone-aware M1 regression.** **(cont. 3) — aggro was a false alarm:** user had set the Giant Rat's faction to +1000 vs humans in a prior test, so it was correctly non-hostile. Not a bug; removed from 3.0.1. Only the thornwood-navmesh re-run remains before resuming Stage C checks. **(cont. 4) — 3.0 VERIFIED + CLOSED (implementation).** User confirmed all Stage C checks pass in thornwood (per-zone mobs wander, corpse-into-zone, per-zone death respawn, login-into-persisted-zone). Marked Stage C ✅ + roadmap **3.0 [x]** (Stages A/B/C all verified in-editor). Two latent bugs fixed this session live in 3.0.1 (mob NetworkTransform, thornwood navmesh persistence — both were masked by prior host-only testing). **NEXT: the one remaining formal gate for 3.0/M-milestone is the full zone-aware M1 regression (`docs/m1-regression-checklist.md`, prepped this session) — an in-editor sweep. After it passes, M1 is closed against the zone-aware world and the next work is 3.1 (starting-zone content) or the other 3.0.1 items (mob rotation sync, per-zone bind points, ZoneSetup NavMeshData hardening already done).** **(cont. 5) — scoped the zone AUTHORING layer (user gut-checked that 3.0-framework alone doesn't give a buildable 3-zone MVP).** Reconciled the user's ask (3 zones, ~3–5 min walk each, a placeable portal prefab that moves players to a string-id'd landing waypoint that also sets arrival facing) against the code: **most of it already exists** — `ZonePortal(targetZoneId,targetEntryId,radius)` + `ZoneEntry(entryId)`, arrival facing already derived from the entry's `eulerAngles.y`, and `ZoneEntry`/`ZonePortal` already draw facing-arrow/radius gizmos. Real gaps = ergonomics + content, not runtime. Confirmed movement is `speed*Time.deltaTime` (walk **1 u/s**, sprint 3), so 3–5 min walk ≈ **180–300 units**; zones stay 5000 apart (float-safe, no bleed). **User decisions:** (1) 3rd zone = **Grukmar's Deep**; (2) basic **flat** terrain of appropriate size, hand-decorate later; (3) keep character speed, **size is the variable**; (4) portals should support **both** cross-zone and intra-zone. **Drafted `docs/devplans/3.0.2-zone-authoring.md`** (🟡 awaiting review, ZA1–ZA7): drag-in `ZonePortal`/`ZoneWaypoint` prefabs + Scene-view `Handles.Label`s, a generalized `BuildFlatZone(...)` + `Tools/Zones/New Zone…` workflow (flat ground + persisted navmesh + default entry + catalog/build-settings registration), intra-zone `ServerMovePlayer` support, and the **3 flat MVP zones** wired into a linear portal graph (thornwood upgraded from the 100×100 stub, Grukmar created). No schema/migration (zones live in `ZoneCatalog`; `zone_id` already persists). Added roadmap **3.0.2**; reshaped **3.1** to "zone content pass" (decorate/populate the scaffolds). **NEXT: user reviews 3.0.2 decisions ZA1–ZA7 → on approval implement (editor-tooling + a small ZoneManager change; no runtime-arch risk). The zone-aware M1 regression can run before or after 3.0.2 — independent.** **(cont. 6) — user approved ZA1–ZA7; implemented 3.0.2 (code only, pending in-editor verification).** ZA5: `ZoneManager.ServerMovePlayer` now supports intra-zone teleports (same-zone target → warp to entry, no scene move / no client scene swap; `sameZone` flag). ZA3/4/6/7: **rewrote `Assets/Scripts/Editor/ZoneSetup.cs`** — generic `BuildFlatZone(zoneId,scene,offset,groundSize,decorate)` with `BakeAndPersistNavMesh` (reuses the 3.0.1 NavMeshData-save fix); `Tools/Zones/Build Zone Scenes` builds all 3 MVP zones (thornwood → full `MvpZoneSize=280u`, **grukmars_deep** new at +10000) + wires the linear portal graph (creslins⇄thornwood⇄grukmar, `default`/`from_*` entries + return portals + a Giant Rat spawn per zone); catalog upserted (preserves others), both non-base scenes added to Build Settings; public `CreateZone(...)` backs the new-zone window; marker placement is prefab-based. ZA1: `Tools/Zones/Create Zone Prefabs` creates `Assets/Prefabs/Zones/ZonePortal.prefab` + `ZoneWaypoint.prefab`. ZA3: new `Assets/Scripts/Editor/NewZoneWindow.cs` → `Tools/Zones/New Zone…`. ZA2: new `Assets/Scripts/Editor/ZoneMarkerGizmos.cs` — `[DrawGizmo]` `Handles.Label`s show portal `→ zone/entry` + waypoint `⚑ entryId` (geometry gizmos already existed on the components). No migration/schema. Grep-verified no dangling refs to the old private ZoneSetup internals. Roadmap 3.0.2 + devplan updated. **NEXT (user, in editor): recompile → `Tools/Zones/Build Zone Scenes` (creates prefabs + 3 zones + graph, persists each navmesh) → SAVE SampleScene → test the 3.0.2 build/test plan (walk creslins→thornwood→grukmar + back, arrival facing per waypoint, isolation across all 3 zones via MPPM, camp/relog into grukmar, a hand-placed intra-zone portal). Tune `MvpZoneSize` for walk-time feel. Independent: the zone-aware M1 regression still pending.** **(cont. 7) — 3.0.2 VERIFIED + CLOSED.** User confirmed the full build/test plan (3 zones build with persisted navmesh + prefab portal graph, walk creslins→thornwood→grukmar + back with correct arrival facing, isolation across all 3 zones, camp/relog into the persisted zone). Marked roadmap **3.0.2 [x]** + devplan ✅ Done; flipped the two 3.0.1 fixes (mob NetworkTransform, zone navmesh persistence) to **✅ FIXED + VERIFIED**. Added a **Zones (M3.0/3.0.2)** architecture subsection + a Current-Status row to this doc. **Remaining open:** 3.0.1 polish (mob rotation sync, per-zone bind points; MPPM quirks are no-fix), the zone-aware **M1 regression** (`docs/m1-regression-checklist.md`, still pending), and **3.1** (decorate/populate the 3 flat zones). All this session's work is uncommitted — user commits. **NEXT: user commits; then choose between M1 regression, 3.1 content, or 3.0.1 polish.** **(cont. 8) — user committed 3.0/3.0.1/3.0.2. DECISION: full M1 regression DEFERRED to pre-first-deployment** (not run per-milestone) — 2.x + 3.0 already re-covered most of it, so a dedicated pass now is redundant; light smoke (boot→login→create→fight→camp→relog) is the interim bar; the zone-aware checklist is kept current + run as a gate before the first real deployment. Updated roadmap (M1.7 note + M3 sequencing note + 3.0 line), `docs/m1-regression-checklist.md` header, the 3.0 devplan Done-when, and the [[project_m1_regression]] memory. **NEXT: next work is 3.1 (zone content/decoration) or 3.0.1 polish (mob rotation sync, per-zone bind points) — user's pick.** **(cont. 9) — user re-scoped 3.1: pivot from "decorate zones" to a NEW-PLAYER ONBOARDING VERTICAL (first polished experience).** Goal: register → login → create a character (2 races × 3 classes) → enter a starting location, through a styled, transition-driven (fades, not snaps) client shell. Decisions: races **Human + Elf** (user imports a Synty pack for the elf; Humanoid rigs → existing clips retarget), classes **Warrior/Wizard/Cleric** with **1 signature ability each** for now (Warrior Kick exists; Wizard nuke; Cleric heal), char-select gets a **live 3D preview**. Deeper class/combat depth is intentionally deferred to two future M4 efforts the user named: **4.1 Combat refinement** then **4.2 Level 1→2 polish round**. **Reshaped `roadmap.md`:** 3.1 → onboarding vertical with sub-items **3.1.1** client screen-flow shell + transitions (foundation; the 1.7-deferred O5 uGUI conversion of login/select), 3.1.2 title page, 3.1.3 register/login screens, 3.1.4 race models+anim (Human/Elf), 3.1.5 class setup (Warrior/Wizard/Cleric, 1 ability each), 3.1.6 char select/create redesign (3D preview), 3.1.7 camp/logout polish, 3.1.8 light starting-location polish; the old "decorate zones" work relocated to **3.6**; added M4 **4.1 combat refinement** + **4.2 level 1→2 polish** (renumbered 4.3–4.7). **Drafted `docs/devplans/3.1.1-client-screen-flow-shell.md`** (🟡 awaiting review, SF1–SF7): a `UIScreenManager` (in `UI.unity`) state machine {Title,Login,Register,CharacterSelect,Connecting,InWorld} deriving the target screen from Mirror state + a `ScreenFader` (CanvasGroup) for fades; retire IMGUI `LoginUI`/`CharacterSelectUI`/`GameNetworkHUD` for uGUI panels reusing the SAME Mirror messages + `AccountAuthenticator` hooks (no server/protocol change); SF6 keeps 3.1.1 to framework + functional parity (styling is the later sub-items). Grounded in the real flow (read LoginUI/CharacterSelectUI/CampController/UIManager). Updated [[project_m1_regression]] earlier this session (regression deferred to pre-deployment). **NEXT: user reviews 3.1.1 decisions SF1–SF7 → on approval implement the shell (client-only UI; no server risk). Then 3.1.2/3.1.3 (styling) and Track B (3.1.4 race models, 3.1.5 classes) can proceed in parallel; char-select redesign (3.1.6) converges last.** **(cont. 10) — user approved SF1–SF7; implemented 3.1.1 (code only, pending in-editor verification).** Built as **runtime-constructed uGUI** (self-contained scripts spawned at startup) — lowest risk untested + purely additive (no SceneSetup/NM-GO/zone re-wiring, no Setup All). New `Assets/Scripts/UI/Screens/`: `ClientScreen` enum + `ScreenPanel` base; `UIScreenManager` (singleton — builds a menu Canvas sort 100 + panels + `ScreenFader`; `Update` derives target screen from Mirror state [localPlayer→InWorld / authenticated→CharacterSelect / client active→Connecting / else pre-connect]; runs faded transitions; centralizes auth glue reusing `AccountAuthenticator` + `LastClientMessage`; **disables the IMGUI `LoginUI`/`CharacterSelectUI` at runtime** so no double-render — kept as files, `GameNetworkHUD` dev-Stop left); `ScreenFader` (black CanvasGroup, unscaled fade); `MenuUI` (TMP + legacy Button/Image factory matching the HUD builder); `TitlePanel` (SF7 stub — bg/logo from `Resources/UI/Title/…` = the art-swap contract), `LoginPanel`, `RegisterPanel`, `ConnectingPanel`, `CharacterSelectPanel` (uGUI port of the IMGUI select, **same Mirror messages**; race/class = cycle buttons for now, styled picker + 3D preview is 3.1.6). `UIManager.Awake` → `UIScreenManager.EnsureExists()` (client-only). Relies on `UI.unity`'s EventSystem (no new one, avoids duplicates). Devplan `docs/devplans/3.1.1-client-screen-flow-shell.md` has the log. **NOT compiled in-editor** — compile-correct by inspection (APIs mirrored from the retired IMGUI). **NEXT (user, in editor): recompile → Play → walk Title→Play→Login/Register→Host(dev)/creds→Connecting→CharacterSelect(create/enter/delete)→InWorld, all fading; camp→fades back to select. Report layout/interaction issues for iteration. Then 3.1.2 (title art), 3.1.3 (login styling), Track B (3.1.4 race models / 3.1.5 classes).**
- 2026-07-03: **Added `/unstuck` command + fixed invisible zone portals (3.0 Stage B polish).** (1) **`/unstuck`** — user walked a player off the map edge into permanent fall; added `NetworkedPlayer.CmdUnstuck` (client `/unstuck` in `ChatUI.TryHandleLocalCommand` → server warp to the current zone's `default` entry via `ZoneManager`, else the bind point; gated out of combat like `/camp`; works even with zones disabled). Also made `ServerWarpTo` zero `_verticalVelocity` so warps/transitions arrive fresh instead of carrying fall speed. Added to the `/help` table. (2) **Portals were invisible** — a `ZonePortal` is just an empty GO with a server-side proximity radius (only a Scene-view gizmo), so the user couldn't find it in-game. Fixes: `ZonePortal` now spawns a **cosmetic cyan pillar** at runtime (`showMarker`, collider-less, non-networked, each peer local); `ZoneManager` poll uses **horizontal distance** (ignores Y) so a y=0 portal still triggers on hilly terrain; `ZoneSetup` moved the creslins portal from a buried `(12,0,12)` to `(0,0,10)` — 15 units due north of the `(0,1.1,-5)` spawn, easy to find. **NEXT (user, in editor): recompile → re-run `Tools/Zones/Build Zone Scenes` (idempotent; repositions the portal + rebuilds thornwood — re-bake its navmesh) → save SampleScene → Host → walk north into the cyan pillar to reach thornwood; then resume Stage B verification (parity / isolation via 2 MPPM clients / transition round-trip). Then Stage C.**
- 2026-07-02 (cont. 2): **3.0 Stage A verified by user; implemented Stage B (isolation + transitions) — code only, pending in-editor verification.** Folded the transition core into B (it's what makes B testable). Two more Mirror-source-grounded findings handled: (1) **`SceneInterestManagement.OnCheckObserver` NREs during character-select** — with `autoCreatePlayer=false` a conn is "ready" at the select screen with no `conn.identity`, and `NetworkServer.SpawnObserversForConnection` still calls `OnCheckObserver` → base impl dereferences `newObserver.identity.gameObject.scene` → **fixed with a project subclass `ZoneInterestManagement : SceneInterestManagement` that null-guards** (used instead of the raw Mirror component; don't edit the package); (2) client scene sync uses Mirror's built-in `SceneMessage` (`conn.Send` LoadAdditive/UnloadAdditive — `NetworkManager.OnClientSceneInternal` handles it; base scene never unloaded). **New:** `Assets/Scripts/Networking/Zones/ZoneManager.cs` (catalog load, additive scene load w/ host-restart re-index guard, entry/portal index, `Update` portal proximity poll + 1s per-conn debounce, `ServerMovePlayer` = `MoveGameObjectToScene`→`SceneMessage`→`ServerWarpTo`→chat-grid resync), `ZoneInterestManagement.cs`, `Assets/Scripts/Editor/ZoneSetup.cs` (`Tools/Zones/Build Zone Scenes`: wires ZoneManager + ZoneInterestManagement onto the NM GO, drops a creslins→thornwood portal + return entry into the active scene, builds/saves `Assets/Scenes/Zones/thornwood.unity` at world offset **+5000 X** with ground + baked navmesh + entry + return portal, writes `Resources/ZoneCatalog.asset`, adds to Build Settings). **Modified:** `GameNetworkManager` (ServerInitialize/ServerShutdown), `NetworkedPlayer.Awake` sets `NetworkTransform.coordinateSpace = World` (the spike's owner-offset lever; no-op single-scene). **Chat zone-key intentionally skipped** — the mandatory 5000-unit Z3-B offsets already isolate the chat grid's 130-unit broadphase, so it's redundant; Stage A's `conn.Send` delivery decoupling was the real fix. **Verified nothing dangling (grep sweep); fixed one API slip (`CoordinateSpace` is namespace-level in Mirror, not nested).** **NEXT (user, in editor): recompile Stage B → `Tools/Zones/Build Zone Scenes` → SAVE SampleScene (gains NM components + 2 markers; additive, doesn't touch terrain) → bake thornwood navmesh if the scripted bake didn't take → test: (1) parity (existing loop still works in creslins), (2) isolation (Server Only + 2 MPPM clients: P1 creslins, P2 walks portal to thornwood → stop seeing each other + mobs), (3) transition round-trip creslins↔thornwood stable. Watch the spike's owner-side offset/sink on the transitioning client (budgeted risk; levers = World coord space + offset). Confirm green, then I implement Stage C (SpawnPoint zone ownership + PlayerCorpse-into-zone + char-select spawn-into-persisted-zone → per-zone mobs + persistence round-trip), then full M1 regression.** Devplan `Docs/devplans/3.0-zone-integration.md` has the Stage B log.
- 2026-07-02 (cont.): **Started implementing 3.0 (zone integration) — landed Stage A (code only; pending in-editor verification).** Resolved the one open decision the approved Z1–Z9 left implicit: **scene model = A** (the existing `SampleScene` **is** the starter zone `creslins_field`; only non-base zones load additively) — informed by the user's new `Assets/Scripts/trellis_zone_design.md` (MVP zones = **Creslin's Field → Thornwood → Grukmar's Deep**, linear; starter = Creslin's Field with Trellis village embedded). Zone ids `creslins_field` (offset 0) + a bare **`thornwood`** stub (offset +5000, real zone not a throwaway). **Grounding against Mirror source found TWO real gaps in the approved devplan, folded in:** (1) `SceneInterestManagement.OnCheckObserver` makes an object visible only to same-scene players → `ChatManager` (base-scene networked singleton) drops its `TargetRpc`/`ClientRpc` chat for players in other zones → **fixed by switching ChatManager delivery to `conn.Send` + a `ChatDeliverMessage` client handler** (the observation-independent pattern `ContentCatalog` already uses); (2) additive zones must be **authored at** their world offset, not runtime-shifted (baked NavMesh stays at authored coords) → `ZoneSetup` builds Thornwood at +5000, `ZoneManager` just additive-loads. Also noted for Stage C: `PlayerCorpse` Instantiate lands in the active scene, not the player's zone. **Staged the work (each stage needs editor/MPPM verification): Stage A (done) = safe foundation** — `0017_character_zone.sql` (adds `characters.zone_id` default `creslins_field`), zone data types in `Assets/Scripts/Networking/Zones/` (`ZoneCatalog`/`ZoneDefinition`, `ZonePortal`, `ZoneEntry`), zone-id persistence plumbing (`CharacterSnapshot`/`CharacterRepository`/`CharacterPersistence`, defaults to starter, behavior-preserving), `NetworkedPlayer.ServerWarpTo`/`CurrentZoneId`/`SetZone`, ChatManager delivery refactor + handler in `GameNetworkManager`. **No `ZoneManager`/`SceneInterestManagement` yet → single-scene game unchanged.** **Stage B** = SceneInterestManagement + Thornwood builder + ZoneManager load/index + chat zone-key (test steps 1–2). **Stage C** = ServerMovePlayer + portal poll + client SceneMessage + SpawnPoint/PlayerCorpse/char-select zone-awareness (steps 3–5), then full M1 regression. Devplan `Docs/devplans/3.0-zone-integration.md` has the full log. **NEXT (user, in editor): let Unity recompile Stage A → `Tools/Database/Run Migrations` (0017 applies; existing chars default to `creslins_field`) → play the normal loop incl. chat (say/shout/whisper/system) — everything must work exactly as before + a save round-trips `zone_id`. Confirm that green, then I implement Stage B.** No `ZoneCatalog` asset needed until Stage B.
- 2026-07-02: **User confirmed 2.7 / 2.7.1 / 2.7.2 in-editor verification passed — all three fully closed (M2 spawn/loot/faction-hit cluster done).** No code/status changes needed: the roadmap and all three devplans were already marked ✅ Done "verified in-editor 2026-06-29" at implementation time; this session just confirms the pending in-editor checks that were the last open thread on them. Also confirmed **there is no `3.1` devplan yet** — 3.1 (Starting zone) is built *within* the 3.0 framework, which is 🟢 Approved but **not implemented**, so 3.1 isn't ready to start (needs 3.0 built + its own reviewed devplan first). **NEXT: implement 3.0 (framework only — real zone content = 3.1; load-on-demand/spawn-snapshots deferred per spike Finding #5) → run full `docs/m1-regression-checklist.md` after it lands → then draft the 3.1 devplan.**
- 2026-06-29 (cont.): **Walked the user through Z1–Z9; all resolved → `3.0-zone-integration.md` now 🟢 Approved, ready to implement.** Confirmed: Z2/Z4/Z5/Z6/Z7/Z8/Z9 inherit the 2.0 spike answers. **Z1 → serialized:** `ZoneCatalog` SO in `Resources/` (boot list id→scene→offset) + in-scene `ZonePortal` markers (position = transform, free); defer DB-backed zones — a zone can't be web-authored end-to-end (scene is a Unity build) so the DB buys little now, and 3.0 stays a framework rework not another content vertical. **Z3 → B (REVERSED my devplan's A rec after deeper analysis):** shared **default auto-stepped** physics scene + **mandatory world-space offsets** per zone. Key reframing — **Unity's runtime NavMesh is a single global mesh; NavMeshAgents ignore physics scenes**, so two zones at the same coords merge → the offset is mandatory *regardless* of physics model, and once zones are far apart the offset already isolates navmesh + every naturally-bounded query (say-radius/OverlapSphere/targeting). So B needs **zero** new physics infra (no `ZonePhysicsStepper`, no per-query `PhysicsScene` redirection) — strictly less code/risk. The spike used isolated physics only because it put both zones at identical coords (0,1,8). **A (isolated per-scene at local origin) is the documented scale-up path** if the world grows past MVP (float-origin jitter at ~100k+ units) — a contained physics/coord-layer swap, not an arch fork. **Decider: user's MVP = 3 zones / ~12 players / "everything working + polished before expanding"** → 3 zones offset a few thousand units = nowhere near float trouble → B clearly correct; A is a "decide on data later" concern (same posture as zone-server deferral to M6b). **Sub-risk to validate FIRST in impl:** mobs navigating in two offset zones simultaneously (confirm global-navmesh offset islands behave). **Get right:** offset spacing (no radius bridges zones; stay in safe float range). Updated devplan throughout (scene load = normal additive + offset; dropped ZonePhysicsStepper from files/types; SpawnPoint/EnemyAI notes; Decisions section). **NEXT: close the 3 pending 2.7.x in-editor verifications → implement 3.0 (framework only; real zone content = 3.1; load-on-demand/spawn-snapshots deferred per spike Finding #5) → run full `docs/m1-regression-checklist.md` after it lands.**
- 2026-06-29: **Drafted the first M3 devplan — `docs/devplans/3.0-zone-integration.md` (🟡 awaiting review, decisions Z1–Z9) — and confirmed nothing blocks it.** User asked to start the first devplan in M3 + whether anything's blocking. **Not blocked:** 3.0's sole hard input is the 2.0 zone spike's go/no-go + integration-surface list, which is ✅ done (GO). Flagged two non-blockers: (1) M2 is mid-flight (2.7/2.7.1/2.7.2 implemented but pending in-editor verification; 2.8–2.12 unstarted) — but that "M3 depends on M2" is about *content to fill a zone* (3.1+), not the zone *framework* (3.0); recommended closing the 3 pending-verify M2 items before 3.0 *implementation*, not before review; (2) the full M1 regression is deliberately deferred to right-after-3.0 (3.0 reworks the M1 systems it covers), light smoke covers the gap. The devplan executes the spike's Finding #4 integration surface: server `ZoneManager` (additive scenes + `SceneInterestManagement`), server-authoritative transitions via `NetworkTransform.ServerTeleport`, **proximity-based portals (no physics triggers)**, `zone_id` column (migration **0017** — next after 0016), move-into-zone-before-`AddPlayerForConnection` at character select, zone-keyed `ChatManager`/`SpawnPoint`; 1.7's additive `UI.unity` already survives zone changes (free). Grounded in real code (read `ChatManager`/`SpawnPoint`/`CharacterSnapshot`/`CharacterSelectController`/`GameNetworkManager`/`UIManager`). **Walked the user through all 9 Z-decisions:** Z2/Z4/Z5/Z6/Z7/Z8/Z9 = adopt-the-spike / obviously-correct low-stakes confirmations; **Z1** = scope call (recommend serialized/`Resources` zone catalog now, defer DB-backed zones — don't gold-plate 3.0 with another content vertical); **Z3 (server physics) = the one real decision** — recommend **(A) physics-isolated per-zone scenes + distinct world-space offsets + manual per-scene `Simulate`** (matches spike's `ZonePhysicsStepper`) over (B) single shared physics scene; **NavMesh-per-zone is the sub-risk to validate FIRST** (global navmesh + per-zone surfaces at distinct offsets should path fine, but test two mobs navigating in two zones before committing). **NEXT: user reviews/approves Z1–Z9 (esp. Z3) → ideally finish 2.7/2.7.1/2.7.2 in-editor verification first → then implement 3.0 (framework only; real zone content is 3.1, load-on-demand/spawn-snapshots deferred per spike Finding #5). Run the full `docs/m1-regression-checklist.md` after 3.0 lands.** No code written — devplan-first, planning only.
- 2026-06-27: **Deprecated-field cleanup pass (user-initiated "clean up as we go").** After 2.7.2 verified, retired now-dead SO authoring fields. **Migrated the scene** first (hand-edited `SampleScene.unity`: added `spawnTableId` = `Mob/Guard/Captain Spawn Table` to the 3 camp SpawnPoints so removing the SO field wouldn't break them — only the rat had been wired, in-memory, for testing). Then removed: **`SpawnPoint.spawnTable` (SO) + `timerOverride` (SO)** — DB spawn tables fully replace them and the server requires a DB anyway, so no fallback value (went beyond devplan DS4's "keep as fallback"); `DoSpawn` precedence is now just `spawnTableId → mobId`. Removed the long-dead **`MobDefinition.conversationKeywordSet` + `vendorInventory`** SO refs (both commented "2.5 removes", never read at runtime) + their tentacles: `MobRegistry` null-assignments, `NpcConversation.EffectiveKeywordSet` fallback term, and the legacy Unity `MobEditorWindow` (its custom conversation/vendor sections rebuilt around those fields → now plain `DrawSection`s showing the DB-bridge ids `conversationSetId`/`vendorId`). **Kept** `MobDefinition.faction`/`.lootTable` — those are *runtime-resolved* (registries set them from the id at load), not author fields. Grep-verified zero dangling refs. ⚠️ User must **reload `SampleScene` in Unity** (don't save over my external edit) + recompile. Orphaned YAML keys (scene `spawnTable`/`timerOverride`, SO-asset `conversationKeywordSet`/`vendorInventory`) are harmless, cleaned on next Unity save. **Deferred (step 3):** deleting the now-unreferenced SO assets (SpawnTable/SpawnTimer; later the SO MobDefinition/VendorInventory/ConversationKeywordSet seed-source assets) — separate careful pass after confirming nothing references them. **Standing convention adopted: when a content type is DB-migrated + verified in-game, remove its deprecated SO authoring fields in the same pass (but never the runtime-resolved ones).**
- 2026-06-26 (cont. 9): **Implemented 2.7.2 (spawn tables + timers) WITH group spawning — both stacks build clean, migration validated; pending in-editor verification.** Closes the spawn-path gap so a camp can be web-authored AND its mobs are full DB mobs (DB loot + faction hits + faction aggro). `0016_spawn_tables.sql` (`spawn_tables` w/ inlined timer base/variance + `spawn_table_entries` children, cascade). Unity: `SpawnTableRepository`/`SpawnTableRegistry` (builds runtime `SpawnTable`+`SpawnTimer`, entries via `MobRegistry.Get`, **loads after mobs**); **`SpawnPoint` reworked** — new `spawnTableId`, precedence **spawnTableId → mobId → legacy SO**, single `_live` → **live-set** `List<NetworkIdentity>`, `DoSpawn` spawns `group_size` mobs (XZ jitter), `OnMemberDied` respawns only when the last group member dies, `ActivationCheck` prunes strays (added `using System.Collections.Generic`); `ContentLoader` spawn step after mobs; `DatabaseSeeder.SeedSpawnTables` (Mob→Giant Rat, Guard→City Guard, Captain→Captain; single weight-1 entries, 30s/0 timer). API `SpawnTablesController` (nested CRUD) + DbContext; Angular `SpawnEditor` (timer + entry rows w/ mob dropdown + weight + group) in nav. `dotnet build` 0/0, `ng build` ok, migration 0016 validated in rolled-back tx (2 tables + FK seed insert). **NEXT (user, in editor): restart API → recompile → `Run Migrations` (0016) → `Seed Database`/host (`[Content] Loaded 3 spawn table(s)`); set the rat SpawnPoint `spawnTableId='Mob Spawn Table'` (clear mobId/SO spawnTable) → restart → rat spawns as DB Giant Rat → kill → DB loot + faction hits fire (THIS unblocks 2.7+2.7.1 in-game verification). Try group_size=3. Then mark 2.7.2 ✅.** Remaining M2: 2.8 quests, 2.9 abilities, 2.10 races/classes, 2.11 seed/export, 2.12 skills (design-first new system).
- 2026-06-26 (cont. 7): **Implemented 2.7.1 (mob faction hits on kill) — both stacks build clean, migration validated; pending in-editor verification.** Makes 2.6's factions consequential + gives the first in-game lever to change a standing. `0015_mob_faction_hits.sql` (`mob_faction_hits` child of `mobs`, cascade). Unity: `MobDefinition.FactionHit` struct + `factionHits` list; **`MobRepository` restructured flat→header-children** (the highest-touch change — added a 2nd query for the hits, every flat column preserved, `LoadAll` still returns `List<MobSnapshot>` so `ContentLoader`/`MobRegistry` callers are untouched); `MobRegistry.Build` resolves each hit's faction via `FactionRegistry.Get`; **`MobKillReward.OnDeath` applies hits via `PlayerFactionScores.ModifyScore` + Reward-channel chat** ("Your standing with X improved/worsened"), killer-only; `DatabaseSeeder.SeedMobFactionHits` (Giant Rat → Vermin −10/CityGuards +2; guards → CityGuards −50, idempotent via INSERT…SELECT…WHERE EXISTS/NOT EXISTS). API: `MobFactionHit` entity + `Mob.FactionHits`, `MobsController` Include(ordered)+replace-children on POST/PUT, DbContext mapping. Angular: `mob.service` Mob gains `factionHits` (+ deep-clone on select), Mob Editor "Faction hits on kill" section (faction dropdown + delta, "+ Hit"/"+ Own faction"). `dotnet build` 0/0, `ng build` ok, migration 0015 validated in rolled-back tx (table + FK + idempotent seed insert). **⚠️ Testing caveat: the in-scene Giant Rat must be on the DB spawn path (`SpawnPoint.mobId='Giant Rat'`) for its DB faction hits to fire — a legacy SpawnTable spawn uses the SO MobDefinition (no factionHits). Proper fix = 2.7.2 (spawn tables→DB).** **NEXT (user, in editor): restart API → recompile → `Run Migrations` (0015) → `Seed Database`/host → kill a DB-spawned Giant Rat → faction chat + Vermin↓/CityGuards↑ in `character_faction_scores` (keyed by faction_id) → relog persists (this also closes 2.6's deferred aggro/gate/round-trip tests). Then mark 2.7.1 ✅. Then 2.7.2 spawn tables (devplan first).**
- 2026-06-26 (cont. 8): **Drafted the 2.7.2 spawn-tables devplan (`docs/devplans/2.7.2-spawn-tables.md`, 🟡 awaiting review, DS1–DS6).** Migrate `SpawnTable` (weighted mob entries + groupSize) + `SpawnTimer` (base+variance) to DB + web Spawn Editor; `SpawnPoint` gains `spawnTableId` (precedence: spawnTableId → mobId → legacy SO). **This closes the 2.7/2.7.1 testing caveat** — a camp pointed at a DB spawn table spawns full DB mobs (DB loot + faction hits + faction aggro). `0016_spawn_tables.sql` (`spawn_tables` w/ inlined timer base/variance + `spawn_table_entries` children, cascade). Unity: `SpawnTableRepository`/`SpawnTableRegistry` (builds runtime `SpawnTable`, entries via `MobRegistry.Get`, **loads after mobs**); `SpawnPoint.spawnTableId` + resolution precedence; `ContentLoader` spawn step after mobs; `DatabaseSeeder` seeds the 3 SO tables faithfully (Mob→Giant Rat, Guard→City Guard, Captain→Captain of the Guard; all single weight-1 entries on the "Fast" timer 30s/0var). API `SpawnTablesController` (nested CRUD); Angular `SpawnEditor` + nav. Key decision **DS3 — group spawning**: recommend implementing it now (you asked for grouped; `group_size` is already authored-but-ignored) — entails reworking `SpawnPoint`'s single `_live` into a live-set (respawn when all group members die); alternative is data-only. Other decisions: DS1 inline timer (retire SpawnTimer SO), DS2 precedence, DS4 retire SO to fallback. **NEXT: user reviews DS1–DS6 (esp. DS3 group-spawn scope) → on approval implement (copy 2.7 pattern; load spawn tables after mobs; watch SpawnPoint precedence for regressions to mobId/legacy paths). Then mark 2.7.2 ✅ — and the rat camp becomes fully DB-driven, unblocking 2.7/2.7.1 in-game verification.**
- 2026-06-26 (cont. 6): **2.7 verified working; M2 planning — added spawn tables (2.7.2) + skills (2.12) to the roadmap.** User confirmed 2.7 loot/XP works in-game. Diagnosed why a web edit to the Giant Rat loot table didn't reflect: the scene's rat/guard/captain SpawnPoints still use the **legacy SO `SpawnTable` path** (`mobId` blank → `spawnTable.Roll().mob` → SO `MobDefinition` → SO `LootTable.asset`), bypassing the DB; only the Merchant SpawnPoint uses the DB `mobId` path. Not a bug — `SpawnPoint` is hybrid by design. Immediate fix for testing: set the rat SpawnPoint's `mobId='Giant Rat'` + clear its spawnTable. **User's broader point: don't abandon spawn tables** → added **roadmap 2.7.2 — Spawn tables + timers** (migrate `SpawnTable`/`SpawnTimer` to DB + web Spawn Editor; `SpawnPoint` references a `spawn_table_id` so weighted/timed/grouped spawning survives, web-authored). **Editor mapping clarified:** race/class editor = already-planned **2.10**; abilities editor = **2.9** (effect-composition — a new ability is pure data via the editor; only a new *mechanic* needs a new `AbilityEffect` subclass, so it's tractable). **"Skill editor" = a NEW system, not just an editor** — no skill scaffolding exists (only a `PlayerAutoAttack` comment "Critical → Solid until skill unlocked"). User chose **EQ-style use-based skills** → added **roadmap 2.12 — Skill system + Skill Editor** (design-first): content half (`SkillDefinition` → DB + web editor, M2-style) + runtime half (per-player skill values, rise-on-use, gating combat rolls incl. the deferred Dodge/Parry/Block + crit hook — M4-flavored combat depth). Needs a design devplan before build. **NEXT: pending — 2.7.1 review (DH1–DH6); 2.7.2 + 2.8–2.12 devplans. User drives sequencing.**
- 2026-06-26 (cont. 4): **Implemented 2.7 (loot tables + XP curve) end-to-end — both stacks build clean, migration validated; pending in-editor verification.** Closes the NPC content cluster (2.3–2.7). Design discussion first: faction-on-kill rewards do NOT belong in the loot table (shared across mobs; items/coin only) — they belong on the **mob** next to `xpReward`, as a per-mob `(faction_id, delta)` **hit list** (EQ-style: killing X moves several factions, not just its own — a pure faction_id derivation can't express "kill X to *gain* standing with Y"). Confirmed nothing calls `ModifyScore` today (faction_id is aggro-only) → split that out as **2.7.1 (mob faction hits on kill)**, deferred. `0014_loot_xp.sql`: `loot_tables` + items/drop_counts/coin_tiers children + single shared `xp_levels` (server-only). Unity: `LootRepository`/`LootRegistry` (builds runtime `LootTable`, items via `ItemRegistry.Get`), `XpRepository` + `PlayerExperience.SetTable` seam (DB→Resources→DefaultValues), `MobRegistry.Build` resolves `def.lootTable` (was hard-null "resolved at 2.7"; `Corpse` already rolls it), `ContentLoader` loads **loot before mobs** + an XP step, `DatabaseSeeder` seeds Giant Rat loot + the 50-level curve faithfully (verified the 3 loot guids → `rat_paw`/`rat_ear`/`rat_whisker`). API: `LootTablesController` (nested CRUD) + `XpController` (GET/PUT) + DbContext mappings. Angular: `LootEditor` + `XpEditor` in nav, mob loot field now a dropdown (closed the "(2.7)" tag); needed `DecimalPipe` import for the XP cumulative column. `dotnet build` 0/0, `ng build` ok, migration 0014 validated in a rolled-back tx (5 tables + FK loot insert + xp insert). **NEXT (user, in editor): restart API → recompile → `Run Migrations` (0014) → `Seed Database`/host (expect `[Content] Loaded 1 loot table(s)` + `50 XP level(s)`); verify a `loot_table_id='Giant Rat Loot Table'` mob drops rat parts/copper at the seeded weights + web-edit reflects after host restart + `MaxLevel==50`. Then mark roadmap 2.7 ✅. Then 2.7.1 (mob faction hits — devplan first).**
- 2026-06-26 (cont. 5): **Drafted the 2.7.1 devplan (`docs/devplans/2.7.1-mob-faction-hits.md`, 🟡 awaiting review, DH1–DH6).** Per-mob `(faction_id, delta)` faction-hit list applied to the killer on death — lives on the mob next to `xpReward` (NOT loot), authoritative explicit list (not derived from faction_id, so it can express "kill X → gain standing with Y"). `0015_mob_faction_hits.sql` (`mob_faction_hits` child of `mobs`, cascade) + demo seed (Giant Rat → Vermin -10/CityGuards +2; City Guard/Captain → CityGuards -50, placeholders). Unity: `MobDefinition.FactionHit` struct + `factionHits` list; **`MobRepository` flat→header-children restructure** (the highest-touch change — currently a flat single-query load); `MobRegistry.Build` resolves hit factions via `FactionRegistry.Get`; **`MobKillReward.OnDeath` applies hits via `PlayerFactionScores.ModifyScore` + chat feedback** (the new runtime wiring). API: `MobsController` gains Include + replace-children for the hit list; Angular: Mob Editor "Faction hits on kill" section (faction dropdown + delta, "+ own faction" convenience). Key decisions: DH2 no auto-spread via relations (explicit only), DH4 killer-only like XP, DH5 seed demo hits so the loop + the 2.6 score-change tests are immediately testable. **This delivers the first in-game way to change a faction score → makes 2.6's deferred aggro/gate/persistence-round-trip tests verifiable.** **NEXT: user reviews DH1–DH6 → on approval implement (copy 2.6/2.7 pattern; watch the MobRepository restructure for 2.5 regressions). Verify: kill a Giant Rat → faction chat + Vermin↓/CityGuards↑ in `character_faction_scores` (keyed by faction_id) → relog persists.**
- 2026-06-26 (cont. 3): **Marked 2.6 ✅ (user accepted; deeper faction-consequence verification deferred) and drafted the 2.7 loot+XP devplan.** User: call 2.6 good, resolve any faction discrepancies later when faction hits on mob-death/quest-completion get wired (3.2+) — there's no in-game way to change a faction score yet (nothing calls `ModifyScore`), so the aggro/gate/round-trip tests aren't meaningful in isolation. Marked roadmap/devplan/log 2.6 ✅. **Drafted `docs/devplans/2.7-loot-xp.md`** (🟡 awaiting review, decisions DL1–DL5): `0014_loot_xp.sql` (`loot_tables` + items/drop_counts/coin_tiers children + single shared `xp_levels` 50-row curve), all **server-only**; `LootRepository`/`LootRegistry` (builds runtime `LootTable`, items via `ItemRegistry.Get`), `XpRepository` + a static seam on `PlayerExperience` (DB table → Resources → DefaultValues fallback); `MobRegistry.Build` resolves `def.lootTable` (currently null "resolved at 2.7"); `ContentLoader` loads **loot before mobs** + an XP step; `DatabaseSeeder` seeds Giant Rat loot + the 50-level curve faithfully from the assets. API `LootTablesController`/`XpController`; Angular `LootEditor`+`XpEditor` + close the mob "(2.7)" loot dropdown (DL5). Key facts grounded from code: `Corpse.cs:25` already rolls `mob.lootTable`; `PlayerExperience` already reads a static `XpTableDefinition`; loot must load before mobs (like factions). ⚠️ Risk: verify the Giant Rat asset's 3 item guids really map to `rat_paw`/`rat_ear`/`rat_whisker` before seeding. **NEXT: user reviews DL1–DL5 → on approval implement (copy the 2.6 pattern: migration → Unity server → seeder → API → Angular; build both stacks + validate the migration in a rolled-back tx). Verify: a mob with `loot_table_id='Giant Rat Loot Table'` drops rat parts/copper at the seeded weights + the XP curve drives leveling from DB. Then mark 2.7 ✅ — completes the NPC cluster (2.3–2.7).**
- 2026-06-26 (cont. 2): **Implemented 2.6 (factions) end-to-end — both stacks build clean, migration validated; pending in-editor verification.** User approved DF1–DF5 and chose **DF2 = re-key faction scores from name → `faction_id` NOW** ("bite the bullet… don't want to deal with it later"). `0013_factions.sql`: `faction_thresholds`/`factions`/`faction_relations`/`race_faction_defaults` (server-only) + the persistence re-key `ALTER TABLE character_faction_scores RENAME COLUMN faction_name → faction_id` with value migration (validated in a rolled-back tx — it migrated **3 real "City Guards" rows** in the dev DB). Unity: `FactionDefinition` gained `FactionId` + `Key` (id-or-name); `FactionSnapshot`/`FactionRepository` (bundled `FactionContent`); `FactionRegistry` rewritten to build runtime `FactionDefinition`/`FactionThresholdTable`/`RaceDefaultsTable` in 3 passes (+`RaceDefaults`/`Thresholds`/`Count`); `PlayerFactionScores` re-keyed to `faction.Key` + prefers `FactionRegistry.RaceDefaults`; `CharacterRepository` uses the `faction_id` column; `ContentLoader` loads **factions before mobs** (mobs resolve `def.faction`); `DatabaseSeeder.SeedFactions` (9 thresholds, 3 factions, CityGuards⇄Vermin hostile, 6 race defaults — faithful to assets). API: `Faction`/relation/race-default/threshold entities + `FactionDto`, `FactionsController` (nested CRUD), `ThresholdsController` (GET/PUT shared ladder), `ContentDbContext` mappings (composite keys). Angular: `faction.service`+`FactionEditor` (relations toggles, race-default rows, shared-thresholds panel) in shell nav; **DF5 closed** — mob `faction` + conversation `required_faction`/`required_standing` are now dropdowns (retired the "(2.6)" tags). `dotnet build` 0/0, `ng build` ok. **No client sync** (factions are server-only; only player score numbers sync). **NEXT (user, in editor): restart API → recompile → `Run Migrations` (0013) → `Seed Database`/host (expect `[Content] Loaded 3 faction(s)`); verify (1) a Vermin-faction mob aggroes a Human (−1000 → KOS), (2) the CityGuards-gated `help` keyword resolves (no "unresolved—ungated" warning), (3) faction-score save/relog round-trips on the re-keyed column. Then mark roadmap 2.6 ✅ and start 2.7 loot.**
- 2026-06-26 (cont.): **2.2–2.5 VERIFIED ✅ — web-authored shopkeeper works in-game.** End-to-end test passed: web-edited Merchant (prefab `Enemy`, conversation `GuardKeywords`, vendor `Merchant`) spawned via a `SpawnPoint` with `mobId=Merchant`; targeting + "wares" opened a shop showing the DB-backed **Tunic** — validating 2.2 client item-sync (Tunic resolves on the client), 2.3 vendors (stock pushed on open), 2.4 conversations, 2.5 mobs in one path. **Two real fixes en route:** (1) the shared `Enemy` prefab lacked `VendorApplicator` (the DB design has most mobs share `Enemy` + differ by `vendorId`) → added `VendorApplicator` + `NpcConversation` to `SceneSetup.PatchEnemyPrefab`'s patch list (inert on non-vendor mobs); (2) the seeded `wares` keyword had `ends_conversation = true` → `NpcConversation.HandleMatch` dispatches the keyword (vendor opens) then immediately `EndConversation` (vendor **closes** + says `closingLine` "Farewell."), so the shop opened+closed in one call (player saw "What would you like?" then "Farewell.", no window). **Rule: a vendor-open keyword must have `ends_conversation = false`.** Fixed the live `GuardKeywords` DB row (UPDATE) + the `DatabaseSeeder.SeedConversations` default. Marked roadmap **2.2/2.3/2.4/2.5 ✅** + devplan verification log appended. **Drafted the 2.6 factions devplan** (`docs/devplans/2.6-factions.md`, 🟡 awaiting review) — server-only, schema `0013_factions.sql` (shared `faction_thresholds` ladder + `factions` + `faction_relations` + `race_faction_defaults`), `FactionRegistry` builds runtime `FactionDefinition` instances in 3 passes, `PlayerFactionScores` prefers DB race defaults, `ContentLoader` factions-step **before** mobs, API `FactionsController`/`ThresholdsController`, Angular `FactionEditor` + close the "(2.6)" dropdowns in mob/conversation editors. Seed mapped faithfully from assets (CityGuards⇄Vermin hostile; race defaults). **Headline risk flagged (DF2): id-vs-name** — `mobs.faction_id`/`required_faction_id` use the asset id ("CityGuards") but `PlayerFactionScores` + 1.3 persistence key scores by `FactionName` ("City Guards"); schema carries both and the runtime `FactionDefinition.FactionName` must = `faction_name`. **NEXT: user reviews DF1–DF5 → on approval implement (copy the 2.5 pattern; verify via a Vermin-faction mob aggroing a Human + the CityGuards-gated `help` keyword resolving).** *(Possible future polish: web Conversation Editor could warn when a keyword both opens a vendor and ends the conversation.)*
- 2026-06-26: **Diagnosed "no items/mobs seeded" during 2.3–2.5 verification — workflow gap, not a bug.** User ran `Tools/Database/Run Migrations` and saw an empty Mob editor + only the manually-added `iron_dagger` item in Angular. Root cause: **Run Migrations applies schema only** (`DatabaseTools.RunMigrations` → `MigrationRunner.Run`); the content seeds (4 items, 4 mobs, Merchant vendor, Captain/GuardKeywords conversations) live in `DatabaseSeeder.Seed`, which **only runs at Host start** via `Database.InitializeServer` (gated by `SeedOnStart`). The 0009/0012 migration `.sql` are schema-only by design. Verified vs the live `ueq_postgres` container: `schema_version` has 0009–0012 applied ✅ but `mobs`/`vendor_inventories` are empty and `items` has only `iron_dagger`. My earlier recipe note ("Run Migrations … seeds items+mobs") was wrong — it conflated the two steps. **Fix added:** new editor menu **`Tools/Database/Seed Database`** (`DatabaseTools.SeedDatabase`) calls `DatabaseSeeder.Seed` directly so content seeds without entering Play mode. **NEXT (user, in editor): let Unity recompile → run `Tools/Database/Seed Database` (or start a Host) → refresh Angular (expect 4 items, 4 mobs, Merchant vendor, 2 conversation sets) → resume 2.3–2.5 in-editor verification (host logs `[Content] Loaded N …`; MPPM client `[Content] Client received N item(s)`; shopkeeper "wares" test), then mark roadmap 2.3–2.5 ✅ and start 2.6 factions.**
- 2026-06-25: **Implemented 2.5 (mobs) — the cluster's testable-shopkeeper milestone.** Mapped 4 SO mobs from asset YAML via guid lookup: Giant Rat (Enemy prefab / faction Vermin / loot "Giant Rat Loot Table"), City Guard (Enemy / CityGuards), Captain of the Guard (Enemy / CityGuards / conversation "Captain"), Merchant (NO prefab + NO conversation — test-data gaps, vendor "Merchant"). Built: `0012_mobs.sql` (full MobDefinition schema, refs by id) + seeded the 4 mobs; Unity server-only `MobSnapshot`/`MobRepository`/`MobRegistry` (builds runtime `MobDefinition`, resolves prefab by name from `NetworkManager.spawnPrefabs`, sets factionId/conversationSetId/lootTableId/vendorId bridges) + `ContentLoader` step; `MobDefinition` gained `factionId`+`lootTableId` bridges (faction/loot SO refs null until 2.6/2.7); **`SpawnPoint.mobId`** spawns a DB mob from `MobRegistry` (else legacy SpawnTable). API `MobsController` (CRUD by mob_id, blank refs→null) **verified vs Postgres via curl**. Angular `MobEditor` added to shell nav (Items/Vendors/Conversations/Mobs) — all sections; conversation+vendor dropdowns, faction+loot text ids tagged (2.6)/(2.7). `ng build` clean. **Shopkeeper test recipe (after in-editor verify):** web-edit seeded Merchant → prefab `Enemy` + conversation `GuardKeywords` (has `wares`); place SpawnPoint mobId=`Merchant`; in-game say "wares" → shop. **NEXT: in-editor verify 2.3–2.5 (restart API → recompile → Run Migrations 0010–0012 → host logs `[Content] Loaded N vendor / N conversation set(s) / N mob(s)` + seeds), mark roadmap 2.3+2.4+2.5 ✅; then 2.6 factions (wires `FactionRegistry` — makes conversation/mob faction gates live).** ⚠️ 2.2 client item-sync STILL unverified in-editor — the shop + mob nameplates lean on it; verify via an MPPM client. User runs API/web from their own terminal now (don't auto-start; I build to scratch + run on :5099 for curl). Devplan `docs/devplans/2.3-npc-content-cluster.md` has 2.3/2.4/2.5 logs.
- 2026-06-24 (cont. 5): **Implemented 2.4 (conversations) + corrected a 2.3 seed gap.** **Correction:** the Glob tool was silently failing on `*.asset` this session — 2.3 wrongly claimed "no vendor assets"; there IS `Resources/Vendors/Merchant.asset` (sells `tunic`). Added an idempotent **Merchant** seed (`DatabaseSeeder.SeedStarterVendors`). Found existing content via `find`/`grep`: Merchant vendor, two conversation sets (Captain, GuardKeywords), faction assets (CityGuards/QeynosGuards/Vermin/DefaultThresholds/RaceDefaults). **2.4 built (conversations are fully server-only — no client sync):** `0011_conversations.sql` (sets + keywords + unlocks, cascades); **seeded Captain + GuardKeywords faithfully from the SO YAML** (mapped guids: Merchant item=`tunic`, GuardKeywords `help` gate=`CityGuards`/Indifferent, `patrol` unlocks `danger`). Unity: `ConversationSnapshot`/`ConversationRepository` (order preserved via shared-list-ref trick) + `ConversationRegistry` (builds runtime `ConversationKeywordSet`) + `ContentLoader` step; `MobDefinition.conversationSetId` bridge (deprecated SO ref); `NpcConversation.EffectiveKeywordSet` resolves by id. **Faction-gate bridge:** added `ConversationKeyword.RequiredFactionId` + server-only `FactionRegistry` stub (empty until 2.6); `MeetsFactionRequirement` resolves by id, treats unresolved gate as ungated + warns once (gates go live at 2.6). API: `ConversationsController` w/ nested DTO (sets→keywords→unlocks) **verified vs Postgres via curl** (POST nested, GET ordered, PUT replace+cascade, DELETE cascade). Angular: `ConversationEditor` added to shell nav (Items/Vendors/Conversations), set list + keyword cards. `ng build` clean. Devplan `docs/devplans/2.3-npc-content-cluster.md` has 2.3 + 2.4 logs. **NEXT: in-editor verify 2.3+2.4 (restart API → recompile → Run Migrations applies 0010+0011 → host logs `[Content] Loaded N vendor / N conversation set(s)` + seeds Merchant/Captain/GuardKeywords), mark roadmap 2.3+2.4 ✅; then 2.5 mobs (the testable shopkeeper milestone — mobs `MobRegistry` + `SpawnPoint.mobId`, references the vendorId/conversationSetId now in DB).** Pattern note: API built to a scratch dir + run on :5099 for curl since the user's :5144 API locks bin while running.
- 2026-06-24 (cont. 4): **Implemented 2.3 (vendor inventories) — first vertical of the NPC cluster.** Approved the cluster devplan (DC1–DC4 as recommended). Built: `0010_vendor_inventories.sql` (vendor_inventories + items child, no seed — Resources/Vendors was empty); Unity server-only `VendorInventorySnapshot`/`VendorInventoryRepository`/`VendorRegistry` + `ContentLoader` step; runtime rewired — added `MobDefinition.vendorId` bridge (deprecated the `vendorInventory` SO ref), `VendorApplicator` resolves vendor by id + validates via `VendorRegistry.Sells` + **pushes the stock id list to the client on shop-open (DC3)**, `VendorUI.Open(vendor, itemIds)` builds the buy list from pushed ids via the 2.2 item registry — **dropped the client `Resources.Load<VendorInventory>`** (key fix: clients had been loading vendor SOs locally). API `VendorsController` (flat `VendorDto` = vendor + ordered item ids; mapping-only EF) **verified vs Postgres via curl** (POST/GET/PUT-replace/DELETE-cascade, order + dups preserved). Angular refactored into a **shell with Items/Vendors nav** (item editor moved to its own component) + new `VendorEditor` (ware rows = dropdowns from the items endpoint, reorder/remove); `ng build` clean. Devplan `docs/devplans/2.3-npc-content-cluster.md` has the 2.3 implementation log. **NEXT: in-editor verify (restart API → recompile → Run Migrations applies 0010 → host logs `[Content] Loaded N vendor inventory(ies)`), then mark roadmap 2.3 ✅; then implement 2.4 conversations (server-only, same pattern).** Vendor stock has no Resources assets to migrate, so nothing was retired/lost. (cont. 2 verification note: user confirmed adding an item via the web Item Editor; in-game item client-sync still pending.)
- 2026-06-24 (cont. 3): **Re-sequenced M2 into an "NPC content cluster" (web editors for vendors/conversations/mobs/factions/loot), per user — they want all the content creators in the Angular app, then test the NPC/vendor loop.** Confirmed vendors ARE already implemented (2026-05-16/17; `VendorApplicator`/`VendorInventory`/`VendorUI`, keyword-driven via "wares") and walked the user through vendor-NPC setup + the Unity "components on a GameObject" concept; flagged a post-2.2 coupling (vendor stock still references SO `ItemDefinition` assets, but item identity now comes from the DB registry → vendor-sold items must have their `item_id` in the DB). **User's scoping answers:** (1) **vendor-NPC slice first** (mob editor starts minimal → testable shopkeeper fast, expand combat/AI after); (2) **pull loot tables + factions into the DB too** (not hybrid string refs — everything web-authored); (3) scene **`SpawnPoint`s resolve a mob by DB id**. Wrote one consolidated devplan `docs/devplans/2.3-npc-content-cluster.md` (covers 2.3–2.7; build/test order vendors→conversations→mob-slice ▶test shopkeeper→factions→loot ▶test hostile mob). **Key simplification found:** factions/loot/conversations/vendor-stock are **server-only** (no Mirror catalog sync needed — only items needed it); mobs reach clients as normal spawned objects. Grounded schemas in the real `MobDefinition`/`FactionDefinition`/`LootTable`/`ConversationKeyword` shapes (all read this session). **Re-sequenced `roadmap.md` M2:** 2.3 vendors, 2.4 conversations, 2.5 mobs, 2.6 factions, 2.7 loot, then 2.8 quests / 2.9 abilities / 2.10 races / 2.11 seed (all unstarted, safe renumber). Decisions in the devplan: DC1 one cluster devplan, DC2 mob prefab = registered spawnable by id (most share Enemy prefab), DC3 vendor stock pushed to client on shop-open, DC4 single shared threshold table. **NEXT: user reviews the cluster devplan → on approval implement vertical-by-vertical starting with 2.3 vendors (copy the 2.2 pattern; server-only so no catalog sync).** Devplan-first still holds — no cluster code written yet.
- 2026-06-24 (cont. 2): **Implemented M2 2.1 (content rails) AND 2.2 (items) in one session.** **2.1 — done & verified & pushed:** reviewed/approved the 2.1 devplan working D2/D5 with the user (D2 = ASP.NET Core **controllers + EF Core mapping-only** — EF Migrations OFF so the StreamingAssets `.sql` runner stays sole schema authority; D5 = **Addressables behind an `AssetResolver` seam**, `com.unity.addressables` 2.6.0 — verified via Unity's registry that 2.3.16 didn't exist). Built `/api` (ASP.NET Core net10.0, `ContentDbContext`, `ContentPingController`), `/web` (Angular 21 standalone), `0008_content_ping.sql`, Unity `ContentLoader`/`ContentPingRepository` seam wired into `OnStartServer`, `AssetResolver`. Verified API CRUD vs live Postgres via curl + both projects build; **user confirmed the full Angular→API→DB→Unity chain in-game** ("saw it ping in game"). Roadmap 2.1 ✅. Fixed two latent issues: `.gitignore` inline-comment leaking `api/appsettings.Development.json` (the DB password), and a leftover smoke-test API process locking port 5144 (kill-by-port the reliable fix). **2.2 — implemented, awaiting in-editor verification:** wrote + approved the 2.2 devplan (D1 = **generic Mirror catalog sync** so clients get content without DB access — the one genuinely new piece beyond copying 2.1; D4 = **retire `ItemRegistry`'s Resources path now**, seeding the 4 existing items idempotently in `DatabaseSeeder`; D2/D3/D5 as recommended — `icon_address` column, one sync-message per type, `item_id TEXT` PK). Built: `0009_items.sql`, `ItemSnapshot`/`ItemRepository`/`ItemRegistry.LoadFrom` (Resources load removed), `ContentLoader` items step, `ContentCatalog` (`ItemCatalogMessage` JSON sync — server sends in `OnServerReady`, client rebuilds registry, host short-circuits), `GameNetworkManager` handler wiring; API `Item` entity + `ItemsController` (CRUD by `item_id`, **verified vs Postgres via curl** incl. 409/400 paths); Angular master/detail **Item Editor** (replaced ping screen). Both stacks build clean. **Partial verification:** user confirmed **adding an item via the web Item Editor** (web→API→Postgres leg works for the user, not just curl); the in-game **client catalog-sync** remains the pending proof. **NEXT (user, in editor): restart the API (`dotnet run`) to pick up items endpoints; reopen Unity → recompile → `Tools/Database/Run Migrations` (applies 0009, seeds 4 items) → Host (expect `[Content] Loaded 4 item(s)`) → MPPM virtual player (expect `[Content] Client received 4 item(s)` — proves the catalog sync, the main risk to watch is client-sync timing) → confirm loot/inventory/equip/vendor resolve DB-backed items. Then mark roadmap 2.2 ✅.** Legacy `content_ping` table/repo/controller left as harmless, defer to a 2.8 cleanup. **Post-2.2 coupling noted (for 2.7):** vendor stock (`VendorInventory.Entries`) still references ScriptableObject `ItemDefinition` assets, but item runtime identity now comes from the DB registry — so any item a vendor sells must have its `item_id` seeded/authored in the DB or the bought item shows blank; migrating vendor stock to reference items by DB `item_id` belongs to 2.7 (mobs/NPC). All work uncommitted for the user's push (user makes all commits). 2.3–2.7 are copies of the 2.2 pattern.
- 2026-06-24: **Returned to a clean working state after the zone spike.** Confirmed the spike teardown was already 95% done: docs landed on `main` (commit `0424d52`), `spike/zone-architecture` branch gone, `Assets/{Scripts,Prefabs}/Spike` deleted (no `.cs` spike refs → compiles clean), `EditorBuildSettings.asset` clean (only `SampleScene` + `UI.unity`, no diff vs HEAD). Only leftover scratch was an orphaned `Assets/Scenes/Spike/ZoneSpikeBootstrap.unity` (+ metas) referencing the deleted spike scripts — **deleted it**; working tree now clean except harness `.claude/settings.local.json`. **Flagged a sequencing mismatch:** user said "start 3.1" but 3.1 (Starting zone) is defined as built within 3.0 (Zone integration), which doesn't exist yet, and all of M2 sits between M1 and M3. **User decided: M2 first (2.1), per roadmap order.** Confirmed the 2.1 devplan (`docs/devplans/2.1-content-platform-foundation.md`) exists but is 🟡 **Draft — awaiting review (decisions D1–D6 carry recommendations)**. **NEXT: user runs `docs/m1-regression-checklist.md` (the still-pending broad M1 sweep, 1.1–1.7+1.6.1; setup gates first → Postgres up + migrations "no pending" 0001–0007 + clean recompile + `UI.unity` in Build Settings) — passing it closes M1. Then review/approve 2.1 devplan D1–D6 → implement (note: 2.1 introduces the Angular SPA + .NET API + content-schema rails, a different stack from the M1 Unity/Npgsql work).**
- 2026-06-22 (cont.): **Started the zone-architecture spike (roadmap 2.0).** Reviewed + approved its devplan (`docs/devplans/2.0-zone-architecture-spike.md`) with decisions R1 dedicated `spike/zone-architecture` branch, R2 minimal capsule player (Q3 answered analytically, no real char-select in sandbox), R3 canonical Mirror mechanism (`SceneInterestManagement` + per-scene `LocalPhysicsMode.Physics3D`), R4 1–2 MPPM-session time-box, R5 findings live in the devplan. **User committed all M1 work to `main` first** (`29cac84`; Synty art committed since repo is private; `_Recovery/` + stray `Assets/Scripts/*.jpg` screenshots gitignored). **New rule: user makes ALL commits — never run `git commit`/`push`** (saved to memory). Scaffolded the throwaway sandbox under deletable `Assets/{Scripts,Scenes,Prefabs}/Spike/` trees: `ZoneSpikeNetManager` (two distinct zones loaded additively, alternate A/B assignment, `ServerMovePlayer` A↔B transition, host-aware), `ZoneSpikePlayer` (minimal capsule, client-auth `NetworkTransformReliable`, kinematic RB, `[ServerCallback] OnTriggerEnter`→transition, `TargetTeleport` reposition), `ZonePortal`, `ZoneDummyMob` (per-zone red/blue tint via SyncVar for visible isolation), `ZonePhysicsStepper` (steps each zone's local physics on server — self-contained copy of Mirror's `PhysicsSimulator`, **no asmdef dep**; without it portal triggers never fire), and `Tools/Spike/Build Zone Sandbox` editor builder (2 zone scenes + bootstrap container + 2 prefabs + Build Settings, re-runnable). Verified all Mirror type/API names against the in-project version. Fixed an `UnityEngine.Input` exception — project uses the new Input System; `ZoneSpikePlayer` now polls `Keyboard.current` (WASD) like `NetworkedPlayer`. **NEXT (user, in editor): run `Tools/Spike/Build Zone Sandbox` → Play → Server Only → 2 MPPM clients; verify isolation (A=red mobs, B=blue, no cross-sight) + one A→B portal transition; then fill the 5 findings in the devplan.** Genuine unknowns to watch (= deliverable Q3): where the player GameObject lives on the *client* across the scene swap, and whether server-side trigger detection fires when the *synced transform* (not `CharacterController.Move`) enters the portal. **First run (Host + 1 MPPM client):** server-side trigger detection WORKS. Found the expected sharp edge — both zones share local coords so both portals sit at `(0,1,8)`; `MoveGameObjectToScene` keeps world position, so a transitioning player lands in the destination's portal and bounces A↔B↔A forever (rapid scene flicker + frozen player). **Fixed:** `ServerMovePlayer` now repositions the player to the entry on the SERVER immediately after the scene move (out of the destination portal) + a 1s per-player transition debounce to absorb the client-authoritative position-sync lag. Also confirmed: running the main editor as **Host** renders both zone scenes' static geometry superimposed (red+blue overlap) — must test as **Server Only** + 2 MPPM clients for a clean view. **Second run (Server Only + 2 clients):** ✅ both players transitioned (A→B, B→A) and ✅ interest isolation confirmed (they see each other only when co-located). Remaining bug: **neither could portal *back* after the first hop.** Root cause = a kinematic Rigidbody stops firing `OnTriggerEnter` after `MoveGameObjectToScene` relocates it to a different physics scene (fresh body fires once; moved body goes dormant). **Fixed by replacing physics-trigger detection with a server-side proximity poll** in `ZoneSpikeNetManager.Update()` (distance ≤ `PortalRadius=2.5` to a cached per-zone portal; debounce still prevents repeat fires). `OnTriggerEnter` removed from `ZoneSpikePlayer`; `ZonePhysicsStepper` + the kinematic RB are now unused-but-harmless (no rebuild needed, recompile only). **Finding for M3 Q4: don't gate zone portals on physics triggers.** **Third run:** return hop STILL failed + new bug — a transitioned player looked sunk halfway into the floor from the *other* client's view. Both traced to **client-authoritative movement fighting server-initiated teleports**: the server-side reposition desyncs `NetworkTransformReliable`'s baseline → observers see an offset position (the sink) AND the server's own transform copy is wrong → the proximity poll (reads server transform) never sees the player reach the portal (return fails). **Fix: switched the spike player to SERVER-AUTHORITATIVE movement** (matches the real `NetworkedPlayer` CmdSendInput model): owner samples WASD → `CmdMove(dir)`; server integrates into the transform each `Update`; `NetworkTransformReliable` default `ServerToClient` syncs to all. Removed `TargetTeleport` (server reposition is now authoritative) + the kinematic Rigidbody (unused). **Finding for M3 Q4: zone players must be server-authoritative; client-auth + cross-scene teleport desyncs position.** Then WASD broke: the prefab's serialized `syncDirection` was still `ClientToServer` (recompile doesn't change a saved prefab), so the client-auth NT overwrote the server's integrated movement → no motion. **Fix: `ZoneSpikePlayer.Awake` now forces `nt.syncDirection = ServerToClient` at runtime** (runs before spawn, Mirror honors it) → no prefab rebuild needed, recompile only. **NEXT: re-test Server Only + 2 clients — confirm WASD works, clean round-trip A↔B↔A both ways, no sink; then fill the 5 findings.** **Isolation audit (user asked "is it safe just b/c branch?"):** verified blast radius — the ONLY modified shipping file is `ProjectSettings/EditorBuildSettings.asset` (builder had *replaced* the build scene list; SampleScene/UI/prefabs/ProjectSettings.asset all untouched, `Spike/` trees fully additive). Fixed the builder's `RegisterBuildScenes` to be **non-destructive** (append spike scenes, keep existing). Restore original list anytime via `git checkout main -- ProjectSettings/EditorBuildSettings.asset` (do at spike end, or re-run builder after to re-add spike scenes alongside). M1 snapshot safe in commit `29cac84`. Spike deletes cleanly: rm `Assets/{Scripts,Scenes,Prefabs}/Spike` + restore build settings. **Run after isolation audit:** transitions worked continuously but each hop the players spawned incrementally HIGHER (Y ratchet) until too high to reach the portal, + X/Z coords drifted. Root cause: I was teleporting via a **raw `transform.position = entryPos` set**, which (per Mirror's own NetworkTransformBase comments) desyncs `NetworkTransformReliable`'s **delta compression** (drift) and makes clients **interpolate toward the far point** (the Y ratchet). **Fix: teleport via Mirror's `NetworkTransformReliable.ServerTeleport(pos, rot)`** (clears snapshot buffer + `Physics.SyncTransforms()` + RPCs a clean jump) — added `ZoneSpikePlayer.ServerWarpTo`, called from `ServerMovePlayer` instead of the raw set. **Finding for M3 Q4: zone transitions must use NetworkTransform's teleport API, never a raw transform set.** Recompile only. **Fifth run:** infinite zoning now stable, BUT a player sees *himself* sunk into the floor **only in the zone he transitioned into** (owner-only + destination-only; observers see him correct → server position is fine). Diagnosis: NT `Apply` sets position identically for owner/observers, so it's the local player coupling to **client-side physics** (the old prefab still had a kinematic Rigidbody + CapsuleCollider) across the scene move. **Fix: make the player a pure NT-driven visual** — builder now strips the `CapsuleCollider` too (RB already removed); `Awake` also sets `_nt.coordinateSpace = World` to align with the world-space teleport. **⚠️ Requires re-running `Tools/Spike/Build Zone Sandbox`** (drops physics from the player prefab; build-settings write is now non-destructive). **Finding for M3 Q4: zone players should carry no client-side physics body, or it desyncs the owner's view across scene moves.** **Sixth run (physics stripped + coordinateSpace=World):** sink PERSISTED → not physics. Now precise: player A renders sunk-half + offset-FORWARD, **constant**, **only in zone B**, **only from A's own client** (B perfect everywhere; observers always see A correct). Key tell: switching Local→World *changed* the symptom (added the forward offset) — impossible for an unparented object (local==world), so **the owner's player copy has an unexpected parent / non-identity target on his own client**. Architecture itself is PROVEN (concurrent isolation + stable infinite transitions both ways); this is an owner-side **presentation** gremlin (integration-flavored). **Stopped guessing — added a TEMP diagnostic** (`ZoneSpikePlayer.LateUpdate` logs local player world/local pos + parent + scene + ntSpace once/sec). **NEXT: read that DBG line from the affected MPPM player's Console while A stands in zone B** (does local≠world? is parent non-null? which scene?) → then a precise fix. The go/no-go is already trending **GO**. **SPIKE CLOSED — user's call: "conceptually Mirror will support my needs; may be challenging to massage the transforms, but it can be done."** Verdict = **GO**. Filled the devplan's 5 findings + go/no-go (devplan status ✅ Done; roadmap 2.0 checked off). Findings that scope the M3 zone-integration item: (1) feasibility GO — concurrent isolated zones + stable transitions; (2) `SceneInterestManagement` + per-scene `Physics3D` additive load, cheap (caveat: local physics not auto-stepped); (3) move = `MoveGameObjectToScene` + `SceneMessage`, but transforms are the work — server-authoritative + `NetworkTransform.ServerTeleport` + no client physics body + an unsolved owner-side coordinate/parent offset (presentation only, left for M3); (4) integration surface = make zone-aware: spawn system, ChatManager grid (per-zone delivery), persistence (add zone id), character-select spawn, per-scene AI/regen ticks, portals via proximity not triggers; (5) spawn-state = start keep-loaded, move to load-on-demand + persisted snapshot later. Removed the temp diagnostic; left the sandbox as a clean reference artifact. **⚠️ Docs (CLAUDE.md, roadmap.md, docs/devplans/2.0) live on branch `spike/zone-architecture` — must land on `main` or they die with the throwaway branch; the `Assets/.../Spike` sandbox code stays on the branch (throwaway).** **NEXT: land the spike docs on main + drop the sandbox; then resume M1 — run `docs/m1-regression-checklist.md`, then start M2.** (M1 regression sweep still pending — decoupled from this spike on purpose so the throwaway couldn't invalidate it.) **Architecture discussion (server-per-zone vs single-server multi-scene):** user asked about EQ-style zone-servers (world server for chat/social + N stripped zone-server processes). Conclusion: valid eventual shape IF scale demands it, but **premature now** — stay single-server multi-scene (just proven), because zone-as-scene and zone-as-server are the SAME abstraction differing only in deployment, so it's not a fork. Recommendation: design **zone as first-class** (zoneId on entities + character state, chat behind a per-zone delivery interface, persistence keyed so a zone could move) to keep the split cheap; revisit with **load data at M6b** (already on roadmap: "Zone server architecture — one process per zone or shared"). Costs that make it premature: Mirror has no server↔server object migration (handoff = client reconnect brokered by a world server) + cross-process chat/group/faction need a message bus + heavy ops for a solo dev. Decide on data, not intuition. **Finalization:** switched to `main` and staged the 3 spike docs (`CLAUDE.md`, `roadmap.md`, `Docs/devplans/2.0-zone-architecture-spike.md`) for commit; sandbox (`Assets/{Scripts,Scenes,Prefabs}/Spike`) stays OFF main — to be deleted (throwaway, never committed) once docs are committed; `ProjectSettings/EditorBuildSettings.asset` to be restored from main (`git checkout -- …`, undo the builder's scene-list overwrite); `spike/zone-architecture` branch deletable after (it's an ancestor of the docs commit).
- 2026-06-22: **M1 complete.** Verified 1.3 (character persistence) in-editor — fixed a pre-existing host-restart UI bug (`InventoryUI`/`EquipmentUI` latched `_bound`, never rebound). Implemented + verified the rest of M1: **1.5** character select/creation (`autoCreatePlayer=false` + `CharacterSelectController`/`CharacterSelectUI`, create=enter, unique names migration `0006`, one char/account, confirmed delete, `_defaultRace/Class` retired to warned fallback); **1.6** multi-character + save policy (migration `0007` drops `UNIQUE(account_id)`; persistence re-keyed account→`character_id` via `CharacterRepository.Upsert`/`Load`/coalesce; id assigned at creation; 8-slot cap; 90s autosave + save-on-quit; "Camp to Character Select"); **1.7** UI architecture refactor in two passes — (A) `LocalPlayer` service (`Spawned`/`Despawned`, fed by `NetworkedPlayer`) with all 8 panels rebound onto it (zero `FindObjectsByType<NetworkedPlayer>` left), (B) HUD moved to additive `UI.unity` (`Tools/Build UI Scene`) loaded by `UIManager` (O5 uGUI conversion of login/select/HUD deferred); **1.6.1** camp/session polish (`/camp`+`/help` command table, 10s cancelable countdown gated by new synced `CombatState` stamped from `Health.TakeDamage`, pulsing HP-frame combat border, chat-clear-on-switch + MOTD). Recurring hotbar-label wiring quirk fixed for good via **runtime self-wiring** in `HotbarUI`/`HotbarSlotUI.Awake` (immune to the edit-time serialization flake; recompile-only). Devplans 1.3–1.7 + 1.6.1 all ✅ in `roadmap.md`. **NEXT: run `docs/m1-regression-checklist.md` (drafted this session) — the broad M1 sweep — then start M2 (DB-backed content + web editors).** All M1 implementation was code+migrations; editor steps per devplan (Setup All / Build UI Scene / Patch Player Prefab / Run Migrations).
- 2026-04-28: Bootstrapped CLAUDE.md and set up auto-update hook. Reviewed full codebase.
- 2026-04-29: Implemented NPC Event System, Faction System, and Conversation System. All three tested and working. Editor setup scripts created for each (Tools menu). `PlayerFactionScores`, `NpcFaction`, `NpcConversation`, `NpcEventDispatcher` all wired via ScriptableObjects.
- 2026-04-30: Implemented `EnemyAI` — NavMesh wander, faction-driven aggro, chase, combat (damage), leash + return, target reassessment after kill, re-aggro fix (perception reset on return), multi-player threat list (`AddThreat` API). Fixed `Health.OnDied` server/client event split. Enemy wander, aggro, and leash all verified working.
- 2026-05-01: Fixed `SpawnPoint` not registering `InvokeRepeating` before host start. Added `Nameplate` (NetworkBehaviour, SyncVar label) + `Billboard` (plain MonoBehaviour, camera-facing rotation) for floating name labels above mobs and players.
- 2026-05-01: Implemented chat/messaging system. `ChatChannel` enum (Say, Shout, Whisper, System, Combat, Reward, Ability, Environment, NPC). `ChatMessage` struct with client-side timestamp. `ChatManager` (NetworkBehaviour singleton) — spatial grid broadphase + Euclidean narrowphase for area delivery; `SendArea`, `SendDirect`, `SendAll`. `ChatUI` — color-coded per channel, per-channel filter toggles, `/say` `/shout` `/whisper <name>` parsing, timestamps (HH:mm:ss, toggleable). Input: Enter opens chat, `/` opens with prefix, Enter submits and closes, Escape cancels. Mouse look (RMB) and LMB+RMB forward movement work while chat is open. Targeting works while chat is open. `SceneSetup` rebuilt: Player/Enemy removed from `Setup All` flow; `Tools/Rebuild Player Prefab` and `Tools/Rebuild Enemy Prefab` added as separate menu items.
- 2026-05-01: Implemented inventory system. `ItemDefinition` (ScriptableObject, `Assets/Ueq/Item Definition`). `InventorySlot` struct + Mirror NetworkWriter/NetworkReader extensions. `ItemRegistry` (MonoBehaviour singleton) — loads from `Resources/Items/`. `PlayerInventory` (NetworkBehaviour) — `SyncList<InventorySlot>` (8 slots), `SyncVar` currency (_copper/_silver/_gold/_platinum, auto-normalized 10:1), `AddItem`/`RemoveItem`/`HasItem`/`IsFull`, `AddCurrency`/`SpendCurrency`. `InventoryUI` — B key toggle (suppressed when chat open), late-binds to local player inventory, slot labels + gold/silver/copper/platinum display, refreshes on SyncList callback. `Setup All` now creates ItemRegistry + InventoryCanvas. Player prefab needs `Tools/Rebuild Player Prefab` to pick up `PlayerInventory`.
- 2026-05-02: Implemented loot system. `LootTable` (ScriptableObject) — weighted item pool, drop count table, coin tier table (min/max copper + weight for bell-curve distributions); `Roll()` returns unstacked slots. `Corpse` (NetworkBehaviour, `IOnDeath`) — activated by `NpcEventDispatcher` on mob death; rolls loot, renames nameplate to "[Mob]'s Corpse", dims visual via `RpcApplyCorpseVisual`; `SyncList<InventorySlot>` + SyncVar copper; despawn timer (5 min) or on empty. `LootUI` — RMB on corpse opens (no targeting required); per-slot Take buttons + Take Coin + Loot All; `CmdTakeLootSlot`/`CmdTakeLootCopper`/`CmdTakeLootAll` on `NetworkedPlayer`. `LootTableEditorWindow` — `Tools/Loot Table Editor`. `ItemEditorWindow` — `Tools/Item Editor`. `CombatLog` component on player + enemy — subscribes to `Health` events, sends directed Combat channel messages. Fixed `PlayerAutoAttack` missing from `CreatePlayer()`. Fixed `Health.TakeDamage` firing both `OnDamaged` and `OnDied` on killing blow. Fixed chat `AppendLine` double-newline bug (blank lines between messages after trim kicked in). RMB loot: `_rmbIsLoot` flag suppresses look mode when RMB press hits an active corpse.
- 2026-05-03: UI polish pass. Chat window: scrollable (`ScrollRect` + `VerticalLayoutGroup` + `ContentSizeFitter`), resizable (`UIResizeHandle` with `ResizeEdge` flags), moveable (`UIDrag`), cursor feedback on hover. Health bar + target frame: `PlayerFrame` / `TargetFrame` components, anchor-based fill bar (`anchorMax.x = pct`), `Health._maxSync` SyncVar so clients see server-computed max. `GameNetworkHUD` replaces `NetworkManagerHUD`, drawn top-right. Player frame, target frame, inventory panel, loot window all moveable via title bar `UIDrag`. Fixed: target clearing when dragging target frame (`IsPointerOverUI()` guard on `TryTarget()`). Fixed: corpse not despawning after loot when no LootTable assigned (always call `CmdTakeLootAll`). `UIDrag.Init()` method avoids AddComponent null-return quirk when GameObject has pointer-event interfaces.
- 2026-05-04: Death handling. `PlayerExperience` (XP stub — `AddXp`/`RemoveXp`/`DeathXpLoss`; level computed from cumulative XP; level 1=1000 XP, level N=N×1000 XP; de-leveling supported). `PlayerCorpse` (standalone spawnable NetworkBehaviour; owner SyncVar; owner-only loot enforcement in commands + RMB detection; XP debt recovered on full looting via `CheckEmpty`; purple visual). `PlayerInventory.ClearAll()` + `TotalCopperValue`. `NetworkedPlayer`: `_bindPoint` (set at spawn, exposed via `SetBindPoint` for future abilities); `HandlePlayerDeath` (spawns corpse, clears inv, applies XP loss, respawns via `TargetRpcRespawn`); loot commands refactored into `TryGetMobCorpse`/`TryGetPlayerCorpse` helpers. `LootUI` dual-type support (Corpse + PlayerCorpse). `SceneSetup`: `PlayerExperience` added to player setup; `Rebuild PlayerCorpse Prefab (Fresh)` menu item; `RegisterSpawnablePrefab` helper called from `Setup All`. **Setup order**: run `Tools/Rebuild PlayerCorpse Prefab (Fresh)`, save to `Assets/Prefabs/PlayerCorpse.prefab`, then `Tools/Patch Player Prefab`, then `Tools/Setup All`.
- 2026-05-04: `PlayerAutoAttack` chat feedback — ON/OFF toggle messages, "no target" variant on toggle, "You cannot see your target" (facing + obstacle LOS check, fires once per swing interval), "Target is out of range" (fires once per swing interval, LOS takes precedence). Restructured Open Items into phased Roadmap (Phase 1–6c).
- 2026-05-04: XP on kill. `MobKillReward` (NetworkBehaviour, `IOnDeath`) — reads `xpReward` from `MobDefinition`; awards directly to attacker on kill; Reward channel messages. `MobDefinition` gains `[Header("Rewards")]` with `xpReward` field. `MobEditorWindow` exposes Rewards section. Enemy prefab setup and patch both include `MobKillReward`. **To activate**: run `Tools/Patch Enemy Prefab` on your Enemy in scene and re-save the prefab, then set `xpReward` on Giant Rat in `Tools/Mob Editor`.
- 2026-05-03: Inventory UI overhauled to tile-based grid. `InventorySlotUI` (MonoBehaviour) — dark background tile, item name + quantity text, `IPointerClickHandler`; serialized `slotIndex`. `InventoryUI` rewired: `slotTiles[]` replaces `slotLabels[]`; held-item state machine (pick up / place / swap / cancel on same-slot click); `CursorItem` GameObject follows mouse via `ScreenPointToLocalPointInRectangle`; LMB on game world (not over any UI) drops held item. `PlayerInventory.MoveSlot` / `DropItem` (server). `CmdMoveInventorySlot` / `CmdDropInventoryItem` on `NetworkedPlayer` (drop sends System chat message). `SceneSetup.CreateInventoryUI` rebuilt for 2×4 tile grid. **Run `Tools/Setup All` to rebuild InventoryCanvas.**
- 2026-05-05: Fixed double-PlayerCorpse bug on player death. Implemented level system: 50-level XP table replacing linear formula; `MobDefinition.mobLevel` field added; Mob Editor shows it. Phase 1 complete. Root cause: two `NetworkedPlayer` instances each registered `HandlePlayerDeath` on `Health.OnDied`, so the multicast delegate fired twice per death. Fix: guard at top of `HandlePlayerDeath` — `if (health.IsImmune) return; health.SetImmunity(10f);` — first invocation sets immunity before spawning the corpse, second invocation exits immediately. Added `Health.IsImmune` property + `SetImmunity(seconds)` server method. `PlayerAutoAttack` now auto-disables when the current target dies instead of silently skipping the swing.
- 2026-05-05: Phase 2 started. `CharacterStats` (NetworkBehaviour) — STR/STA/AGI/DEX/INT/WIS/CHA; server-side base + equipment bonus fields; SyncVar totals for client UI; `SetRaceClass` / `AddEquipmentBonus` / `RemoveEquipmentBonus`. `RaceDefinition` and `ClassDefinition` gain per-stat fields. `CombatStats` ScriptableObject retired — `PlayerAutoAttack` uses inline serialized fields, `Health` uses serialized `maxHealth` fallback. `PlayerExperience.ApplyModifier` forwards to `CharacterStats.SetRaceClass`. Race & Class Editor gains Stat Modifiers (races) and Base Stats (classes) sections. Six calculation stubs added to Phase 2 roadmap for ideation: HP, mana, combat damage, aggro/threat, HP regen, mana regen.
- 2026-05-06: Ideated HP, mana, and combat calculation formulas. HP formula locked: 5-field per-class model; level 50 geared endpoints: melee 2143 / hybrid 1349 / healer 966 / caster 672 HP. Mana formula locked: same 5-field shape; INT vs WIS per class; warrior/monk/rogue have no mana (warrior abilities free with complex cooldown); level 50 geared endpoints: caster 2095 / healer 1845 / hybrid 1015 mana. Combat calculation locked: Might/Finesse weapon categories; ATK derived from STR+DEX weighted by category; bell curve hit system (2.5/22.5/50/22.5/2.5 base distribution) shifted by ATK-AGI differential; hit multipliers Miss/Glancing/Normal/Solid/Critical; AC mitigation as AC/1000 fraction; critical hit skill-trained. HP regen locked: 1/tick standing, 2/tick sitting (sitting deferred), 6s per-player server tick. Mana regen same model. Aggro/threat deferred to grouping phase.
- 2026-05-06: Implemented HP calculation, mana calculation, combat calculation, HP regen, mana regen. `Health.EffectiveMax` dynamically computes from class formula; `ClassDefinition` gains HP + mana formula fields + `ManaStatType` enum; `Health.RefreshMax()` + `_maxSync` hook keep clients in sync on level-up. New `PlayerMana` (NetworkBehaviour) for mana pool. New `PlayerRegen` (NetworkBehaviour) — 1 HP + 1 mana per 6s server tick. `PlayerAutoAttack` overhauled: `WeaponCategory` enum, ATK from stats, hit roll with shift factor, type multipliers with variance, miss sends combat chat message; Critical fires as Solid (skill system future). Race & Class Editor gains HP Formula + Mana Formula sections. `CombatStats` fully purged — `EnemyAI` and `CombatSetup.cs` cleaned up. **Pending editor setup before testing**: run `Tools/Patch Player Prefab`, configure ClassDefinition HP/mana fields in Race & Class Editor, assign `_defaultClass` on `PlayerExperience` in player prefab.
- 2026-05-10: Fixed duplicate components on Player prefab. `PatchComponents` now deduplicates before adding; added `Tools/Fix Duplicate Components (Player)` standalone menu item for cleaning existing prefabs.
- 2026-05-15: Fixed compilation error — `PlayerController.cs:12` had `0.1` (double literal) assigned to a `float` field; corrected to `0.1f`.
- 2026-05-15: Implemented equipment system. `EquipSlot` enum (Head→Offhand, 13 slots). `PlayerEquipment` (NetworkBehaviour, `SyncList<string>` per slot, `TryEquip`/`TryUnequip` with stat bonus wiring and HP/mana max refresh). `ItemDefinition` gains equip flag, `EquipSlot`, per-stat bonuses, and weapon stats (`weaponBaseDamage`, `weaponDelay`, `weaponRange`, `weaponCategory`). `PlayerAutoAttack` reads from equipped weapon, falls back to serialized inspector values. `InventorySlotUI` RMB → equip via `CmdEquipItem`. `EquipmentUI` (E key, draggable panel) + `EquipSlotRowUI` for unequip. `SceneSetup`: `PlayerEquipment` added to player setup, `CreateEquipmentUI()` wired into `SetupScene`. **Setup**: run `Tools/Patch Player Prefab` to add `PlayerEquipment`, then `Tools/Setup All` to create the equipment window. Mark item as equippable in `Tools/Item Editor` (or Inspector), set `EquipSlot`, then add it to a loot table or give it via inventory.
- 2026-05-16: Implemented vendor/shop system. `VendorInventory` (ScriptableObject, `Assets/Ueq/Vendor Inventory`) — list of `VendorEntry` (item ref); save under `Resources/Vendors/`. `VendorApplicator` (NetworkBehaviour, `IOnConversationKeyword` + `IOnConversationEnd`) — attach to NPC; SyncVar `_vendorId`; `TargetRpcOpenShop`/`TargetRpcCloseShop` sent to the player on keyword match/conversation end. `VendorUI` — Buy/Sell tabs; dynamic rows built at runtime; buy/sell prices in formatted PP/GP/SP/CP; refreshes sell list on inventory change. `CurrencyUtil.Format(int copper)` shared utility. `ItemDefinition` gains `buyPrice`/`sellPrice`. `CmdBuyItem`/`CmdSellItem` on `NetworkedPlayer`. `Item Editor` now shows Equipment, Stat Bonuses, Weapon Stats, Economy sections. `Tools/Setup All` creates VendorCanvas. `Tools/Editor/Create Vendor Inventory` bootstraps new asset in `Resources/Vendors/`. **Setup**: create `VendorInventory` asset via Tools menu, populate with items (set `buyPrice` in Item Editor), save in `Resources/Vendors/`. Add `VendorApplicator` + `NpcConversation` + `NpcEventDispatcher` + `Health` + `Targetable` to a vendor NPC GameObject. Add "wares" keyword (passive or active) to the NPC's `ConversationKeywordSet` with response text. Set `VendorApplicator._vendorInventory` to your asset. Run `Tools/Setup All`.
- 2026-05-17: Fixed vendor setup gap — `MobDefinition` gains `vendorInventory` + `vendorOpenKeyword` fields; `VendorApplicator.OnStartServer` now reads config from `MobApplicator` instead of its own serialized fields; Mob Editor gains Vendor section. Vendor config is now fully driven by the Mob Editor — no manual Inspector edits needed on `VendorApplicator`.
- 2026-05-24: Designed and implemented ability system. `AbilityTag` (pure semantic label), `CooldownLink` struct (tag + duration, empty → GCD), `AbilityDefinition`, `AbilityEffect`/`DamageEffect`/`HealEffect`, `AbilityRegistry`, `PlayerAbilities` (hotbar + GCD/linked-timer engine), `HotbarUI`/`HotbarSlotUI` (keys 2–9), `AbilityEditorWindow`, `AbilityTagEditorWindow`, Race & Class Editor Known Abilities section, SceneSetup wired for all. Dev plan at `docs/devplans/ability-system.md`.
- 2026-05-27: Fixed hotbar empty-on-spawn bug. `PlayerAbilities.SetRaceClass` only populated `_knownAbilities`, never `_hotbar`. Fix: `OnStartServer` re-calls `SetRaceClass` after initializing the SyncList (component order: `PlayerExperience` fires before `PlayerAbilities`, so first call was a no-op against empty list); `SetRaceClass` now auto-fills hotbar slots 0..N-1 from starting abilities using `_hotbar.Count` bounds to survive early calls.
- 2026-05-28: Fixed `InvalidOperationException` in `PlayerAbilities.Update` — iterating `_linkedTimers.Keys` while writing values via indexer invalidates the enumerator in Mono. Fix: pre-allocated `_linkedTimerKeys` list, cleared and repopulated each tick before iterating. Hotbar still showing partial wiring in scene (alternating blank slots) — needs `Tools/Setup All` + scene save.
- 2026-06-17: Guidance session on importing Synty Polygon Adventure art (`.unitypackage`). No code changes. Notes for next time: project is Unity 6 (6000.4.0f1) + URP 17.4 — old `Edit/Window → Rendering → Materials → Convert…` menu paths don't exist; URP material conversion in Unity 6 is `Window → Rendering → Render Pipeline Converter` (Built-in → URP, Material Upgrade), or multi-select Synty materials and swap shader to URP/Simple Lit. Plan: import to its own top-level `Assets/Synty/…` folder, treat as vendored art, then wire a Synty character mesh onto `Player.prefab`. Pack imported fine to `Assets/Synty/PolygonAdventure/` (+ PolygonGeneric, SyntyPackageHelper); materials already URP-ready (no magenta, no conversion needed). **Player visual swap plan**: `Player.prefab` root IS a primitive Capsule (`SceneSetup.CreatePlayer`, height 2/center 0 → feet at local y=-1); remove root MeshFilter+MeshRenderer, parent a Synty character (from `Characters.fbx`) at local (0,-1,0), keep all net/CC components on root, Synty child gets no collider/net comps. Re-running `Tools/Rebuild Player Prefab` would clobber it. **Animation**: `Characters.fbx` rig is Humanoid (`animationType: 3`) so any humanoid clips retarget; but pack ships ZERO animation clips — bundled `Character.controller` references clips by dead GUIDs. Need a clip source: recommended **Synty ANIMATION – Base Locomotion** (247 humanoid clips, made for POLYGON, includes Mecanim controllers) or free Mixamo (identical retargeting for basic idle/walk/run). Next step once clips exist: write networked `PlayerAnimator` driver that reads synced transform movement speed (NetworkTransformReliable syncs pos to all clients → no NetworkAnimator/Cmd needed) and feeds a blend-tree Animator `Speed` param. **Chose free Mixamo route.** Implemented: `Assets/Scripts/PlayerAnimator.cs` (MonoBehaviour, `[RequireComponent(Animator)]`, drives `Speed` float from horizontal world-pos delta, applyRootMotion off, OnEnable resets baseline so respawn teleport doesn't spike) + `Assets/Scripts/Editor/PlayerAnimatorSetup.cs` (`Tools/Build Player Locomotion Controller` — scans `Assets/Animations/Locomotion` incl. clips nested in FBX, matches idle/walk/run by filename keyword, builds 1D blend tree controller at `Assets/Animations/PlayerLocomotion.controller`, thresholds 0/1/3 to match moveSpeed/sprintSpeed). 15 ready Synty char prefabs at `Synty/PolygonAdventure/Prefabs/Characters/` (Knight/Viking/Warrior/Peasant/Shopkeeper). **Pending user (editor/browser):** download Idle/Walk/Run from Mixamo (FBX for Unity, Without Skin, In Place for walk+run) → `Assets/Animations/Locomotion`, set Rig=Humanoid + Loop Time, run the Tools build, do the Player.prefab mesh swap (remove root MeshFilter/MeshRenderer, parent Synty char at local (0,-1,0)), assign controller + add PlayerAnimator to the Synty child. Not yet tested in editor.
- 2026-06-17 (cont.): Animator bug — character only showed rest pose, no idle/walk/run. Cause: `PlayerAnimatorSetup` matched clips on the AnimationClip's internal name, but Mixamo names every embedded clip `mixamo.com`, so all 3 blend-tree nodes built empty (`m_Motion: fileID 0`). Fix: match on source FBX filename instead (`Idle`/`Walking`/`Running`), excluding "back" so `Walking` beats `Walking Backwards`. User must re-run `Tools/Build Player Locomotion Controller`. If walk/run still fail after, next suspect is the `Speed` param not climbing (watch live in Animator window).
- 2026-06-17 (cont. 2): After clips wired, character went to T-pose ("arms out"). Mixamo clips confirmed Humanoid (`animationType: 3`), so cause is the Synty child Animator's **Avatar = None** — humanoid clips have no skeleton to retarget onto → bind/T-pose. Fix: assign the avatar generated by `Characters.fbx` (`Assets/Synty/PolygonAdventure/Models/Characters/Characters.fbx`, person icon, ~`CharactersAvatar`) to the Animator's Avatar field; also set Controller=PlayerLocomotion, Apply Root Motion off, Culling Mode=Always Animate. Fallback if still T-posing: parent the full `SM_Chr_*.prefab` (ships correctly-bound Animator+Avatar) instead of a bare mesh, then just swap Controller + add PlayerAnimator. Awaiting user confirmation of whether they parented a bare mesh or an `SM_Chr_*` prefab. Confirmed hierarchy is CORRECT: Synty char is a child of the player root (root keeps CC + net + Player* scripts, capsule MeshRenderer disabled), which is the intended setup — T-pose is the Avatar field, not the hierarchy.
- 2026-06-18: Continued T-pose debug. Rebuild fixed the empty blend tree (clips now wired at thresholds 0/1/3). User reports Avatar/Controller/RootMotion all set, still T-pose. Findings from files: `CharactersAvatar` (Characters.fbx, guid fd7d68a6, fileID 9000000) IS the correct avatar — same one `SM_Chr_*` prefabs reference, so avatar is NOT wrong (earlier advice to "fix the avatar" was a red herring). Critical: saved `Player.prefab` AND `SampleScene.unity` contain ZERO Animator/SkinnedMesh/Synty refs → user's Synty child setup is UNSAVED (likely the real issue). `SM_Chr_*` prefabs are self-contained (own Animator+correct Avatar+65-bone skeleton). Recommended clean rebuild: delete hand-built child, drag `SM_Chr_Knight_01_White.prefab` as child at (0,-1,0), set ONLY Controller=PlayerLocomotion (leave Avatar), add PlayerAnimator, **Ctrl+S the prefab**. If still T-pose, runtime diagnostic via Animator window (Speed climbing? blend dot moving? state playing?) to split driver-vs-clip-vs-controller. Awaiting result.
- 2026-06-19 (cont.): Hooked the Kicking animation to the Kick ability via a new **data-driven per-ability animation trigger** (scales to all abilities). `AbilityDefinition.animTrigger` (string, empty = none). `PlayerAbilities.TryCast` fires `RpcPlayAbilityAnim(animTrigger)` (ClientRpc → local + remote) after effects apply; caches `PlayerAnimator` via `GetComponentInChildren`. `PlayerAnimator.PlayTrigger(name)` — generic trigger, guards against params the controller doesn't declare (no warning spam). `PlayerAnimatorSetup` now also builds a **Kick** state from `Kicking.fbx` (matched on "kick"); refactored the Attack/Kick one-shot state creation into shared `AddTriggeredState` helper (Any-State → state on trigger, → Locomotion at 90%). `AbilityEditorWindow` gains an Animation section. `Kick.asset` set `animTrigger: Kick`. Warrior's startingAbilities are Kick + Taunt, so Kick sits on hotbar slot 0 (cast key `2`). **Manual step**: re-run `Tools/Build Player Locomotion Controller` to regenerate the controller with the Kick state (logs `kick:Kicking`). Note: `Kick.asset` has `effects: []` — animation + 10s cooldown only, no damage until a `DamageEffect` is added. Still imported-but-unwired: `Jump.fbx`, `Walking Backwards.fbx`, `One Hand Sword Combo.fbx`. **Kick verified working in play mode.**
- 2026-06-20: Added Synty grassland terrain tooling. New `Assets/Scripts/Editor/TerrainSetup.cs` — `Tools/Terrain/Build Synty Grassland` (and `Clear Synty Terrain`). Replaces the grey primitive ground + obstacle cubes with: tiled `SM_Gen_Env_Ground_Grass_Large_01/02/03` (footprint measured at build time for seamless tiling), scattered trees/rocks (colliders kept) + bushes/flowers/grass tufts (colliders stripped, decorative), and a hand-placed village cluster (`SM_Bld_Village_*`, `Hut`, `Well`, `Stall`) centered at (28,0,24). Navmesh design: the flat "Ground" plane is KEPT as the walkable collider (renderer turned OFF so grey hides under the Synty visuals at y≈0.02); `NavMeshSurface.m_UseGeometry` is set to **PhysicsColliders** so the bake comes off that flat collider → guaranteed full-coverage flat navmesh regardless of tile gaps / collider-less props; tall props with colliders still carve it; decorative foliage root gets a `NavMeshModifier(ignoreFromBuild)`. Deterministic (fixed seed 20240620), re-runnable (clears prior `SyntyTerrain` + `Obstacles`), keeps a clear radius (12) around player spawn and around the village (15). Prefab folders: Generic `PolygonGeneric/Prefabs/Environment/`, Adventure `PolygonAdventure/Prefabs/Environments/` (note plural) + `Buildings/`. **Workflow**: run AFTER `Tools/Setup All` (Setup All recreates grey Ground+Obstacles), then bake — select Ground, click Bake on the NavMeshSurface component (Window > AI > Navigation). Tunable via constants/counts/village layout array at the top of TerrainSetup.cs. Not yet run/verified in-editor. **Update**: user opted for a different approach they found and didn't use the tool — `TerrainSetup.cs` is left in place as an available option but is unused/unverified. Revisit only if the manual route doesn't pan out.
- 2026-06-20 (cont.): User hand-placed Synty environment meshes (hills/ground) and reported "sinking into the hills" when walking. Cause: scene has many Synty meshes but only 1 MeshCollider and no Unity Terrain — the hills are visual-only, so the player's CharacterController (`NetworkedPlayer.ApplyMovement`, gravity + `_cc.Move`) walks on the flat ground plane beneath and sinks through the hill geometry. Fix: added `Tools/Terrain/Add Mesh Colliders to Selection` to `TerrainSetup.cs` — recursively adds a non-convex `MeshCollider` to every `MeshFilter` under the selected object(s) that lacks a collider (skips already-collidable; re-run safe). Workflow: select hill/ground roots → run. Alternative noted: enable "Generate Colliders" on the source FBX import. Caveats flagged to user: don't add colliders to decorative foliage; steep hills may exceed the CharacterController `slopeLimit` (45°, not yet exposed on the player); enemy navmesh won't follow hills without a re-bake including the new geometry.
- 2026-06-20 (cont. 4): Wrote + approved the first devplan, `docs/devplans/1.1-postgres-docker-env.md` (roadmap item M1 → 1.1). Status ✅ approved, ready to implement. Approach: Postgres in Docker (`docker-compose.yml` + `.env`), **raw Npgsql** driver in the Unity server (NO ORM — EF rejected as AOT-hostile under Unity, Dapper deferred to 1.2 only if mapping gets tedious), homegrown `.sql` migration runner with a `schema_version` table, `.sql` files in `StreamingAssets/Database/Migrations/`, seed hook, all wired into `GameNetworkManager.OnStartServer` (abort server start on DB-down). Approved defaults D1–D5: NuGetForUnity for Npgsql, Npgsql **6.x** (netstandard2.0), homegrown runner, abort-on-down, StreamingAssets. **First implementation step next session: Npgsql driver smoke-test in Unity** (verify API Compatibility Level = .NET Standard 2.1 and a bare `NpgsqlConnection.Open()` links) BEFORE building anything on top. No code yet — planning only.
- 2026-06-20 (cont. 3): Roadmap + process setup. Created `roadmap.md` (repo root, authoritative forward plan) with milestones M1 Identity & Persistence (current) → M2 Content Authoring Tools → M3 World & Content → M4 Gameplay Depth → M5 Deploy & Scale (AWS). Decisions locked: **PostgreSQL in Docker** locally (seeded as we go, AWS later — chose Postgres over SQLite for clean migration); security deferred (KCP unencrypted ok for hobby stage); async save queue for DB writes off the server tick. **Devplan-first workflow**: every roadmap item needs a reviewed devplan in `docs/devplans/<id>-<slug>.md` before implementation. Numbering: `milestone.item` (1.1, 1.2) with a third dotted level (1.2.1) for work inserted between items so nothing renumbers. Added `docs/devplans/README.md` (convention). `CLAUDE.md` Roadmap section now points to `roadmap.md` as authoritative. Next: write devplan for **1.1 — Postgres + Docker dev environment**. No code changes this session.
- 2026-06-20 (cont. 2): Made mob spawns respect the hills. `SpawnPoint.cs` now resolves spawn position via new `ResolveSpawnPosition()` instead of raw `transform.position`: raycasts down (`Vector3.up*50` origin, 100 dist, serialized `groundMask`, ignore triggers) onto the terrain surface, then `NavMesh.SamplePosition(pos, navSampleRadius=8, AllAreas)` to snap onto the navmesh so the `NavMeshAgent` is valid and sits at hill height. New serialized fields: `snapToGround` (true), `groundMask` (~0/Everything), `navSampleRadius` (8) — existing SpawnPoints inherit defaults. Added `using UnityEngine.AI;`. Also added `Tools/Terrain/Rebake NavMesh` to `TerrainSetup.cs` — invokes `NavMeshSurface.BuildNavMesh()` (via reflection, `FindType` + `Object.FindObjectsByType`) on every surface in the scene. Workflow: ensure hill colliders exist → `Tools/Terrain/Rebake NavMesh` → position spawn points over hills (XZ; Y auto-resolves) → play. Order matters: rebake BEFORE play or the spawn snap samples a still-flat navmesh. Caveat: steep hills exceeding NavMesh Max Slope (45°) get no navmesh → spawn pulls to nearest flat point (hill base); raise Max Slope on the NavMeshSurface + rebake if needed. EnemyAI/NavMeshAgent placement already had a `TrySetDestination` Warp-to-navmesh fallback so no change needed there.
- 2026-06-19 (cont. 2): Fixed T-pose-while-moving regression triggered by re-running `Tools/Build Player Locomotion Controller`. Root cause: the player's controller was assigned as a prefab **override on the nested SM_Chr Animator** (`fileID 9536910` on GameObject `113364`, shared with `PlayerAnimator`); `AnimatorController.CreateAnimatorControllerAtPath` recreating the `.controller` asset (GUID `1f50137d` + content preserved — confirmed identical) left that override's objectReference stale, so Unity dropped it on reload → Animator loaded with `m_Controller = None` → humanoid bind/T-pose while CharacterController still moved. Console tell: "Animator is not playing an AnimatorController" from `PlayerAnimator.Update` `SetFloat` (every frame). Fix: made `PlayerAnimator` robust — new `[SerializeField] RuntimeAnimatorController controller` field, assigned to the Animator in `Awake` if it has none (self-heals a dropped override), and `Update` early-returns when `runtimeAnimatorController == null` (stops per-frame error spam). **Manual step done by user**: assigned `PlayerLocomotion` to the new PlayerAnimator `Controller` field (and re-assigned it on the Animator) on the Synty child in `Player.prefab`, saved. Lesson: prefer assigning the controller via `PlayerAnimator.controller` (same component, reassigned at runtime) over the fragile nested-Animator override.
- 2026-06-20 (cont. 5): **Major direction clarification + started implementing 1.1.** Direction: content goes IN Postgres, authored via **WEB editors** (not Unity SOs) — classes/abilities/skills/races/quests/items (esp. quests+items), XP thresholds, loot tables, etc. Consumption model decided: **DB-at-startup** (server loads content into existing registries on boot; DB is source of truth; pure data in DB, Unity asset refs as string ids resolved via Resources/Addressables; hybrid only as migration path). **Web stack decided: Angular SPA + lightweight C# (.NET) API + Postgres, NO auth for now** (Angular for user's familiarity + complex authoring like quest wizards; brother has no SE experience → web editors are how he authors). Rebuilt **M2 in `roadmap.md`** → "DB-Backed Content & Web Editors": 2.1 platform foundation, 2.2 items (reference vertical), 2.3 quests, 2.4 loot+XP, 2.5 abilities, 2.6 races/classes, 2.7 mobs/NPC, 2.8 seed/export. Locked principle: migrations own schema, web tool only touches data. **Implementation of 1.1 begun**: `docker-compose.yml` + `.env`(+example) → Postgres 16 container **up & verified healthy** (SELECT version OK); `.gitignore` updated (.env, db.config.json, /Assets/Packages/); **NuGetForUnity** added to `Packages/manifest.json` (git URL); `Assets/packages.config` pins Npgsql 6.0.11; `Assets/Scripts/Editor/DatabaseSmokeTest.cs` (`Tools/Database/Smoke Test (Npgsql)`, guarded by `UEQ_NPGSQL_SMOKETEST` define). Project API compat already `.NET Standard 2.1` (no change needed). **Npgsql transitive-dep cascade (the expected risk)**: NuGetForUnity restore of a hand-edited packages.config does NOT pull deps, so added them explicitly: System.Text.Json 6.0.10, System.Text.Encodings.Web 6.0.0, System.Threading.Channels 6.0.0, System.Diagnostics.DiagnosticSource 6.0.1, and (last) **Microsoft.Bcl.AsyncInterfaces 6.0.0** (referenced by System.Text.Json, not by Npgsql's netstandard2.1 build — found via `GetReferencedAssemblies` reflection). Errors walked down: 3 missing refs → restart cleared 2 → "Reference has errors 'System.Text.Json'" → its missing `Microsoft.Bcl.AsyncInterfaces` just added. **NEXT: user does NuGet→Restore + reimport, confirm Console clean, add `UEQ_NPGSQL_SMOKETEST` define, run the smoke test (expect Postgres 16.13 dialog). If green → build real 1.1 (DatabaseConfig→MigrationRunner→schema_version→seed→`GameNetworkManager.OnStartServer`) and delete the throwaway smoke test.** Note: Unity won't act on hand-edited packages.config additions reliably — installing via NuGet→Manage window auto-resolves the whole closure and is the canonical path if more deps surface.
- 2026-06-21: Planning session — drafted devplans for **1.3 and 1.4**, and **re-sequenced M1 so 1.4 (accounts + login) now precedes 1.3 (character persistence)** per user review. Started by writing `docs/devplans/1.3-character-state-persistence.md` (persist the mutable set — XP/currency/inventory/equipment/faction scores/hotbar/current HP+mana/position+bind/race+class id — and **reconstruct everything derived**: stats, max HP/mana, known abilities, level). Review decisions on 1.3: **O1 → defer identity until after 1.4** (no placeholder key; key off the real account→character mapping → this is what flipped the build order), **O2 → normalized child tables** (characters + character_inventory/_equipment/_faction_scores/_hotbar, FK to account), **O3 → persist current HP/mana**, **O4 → add `RaceClassRegistry`** (move Race/Class assets under `Resources/` for name→asset resolution; M2.6 later swaps for DB loader), **O5 → drop `dal_smoke`** in the character migration. 1.3 devplan marked ⛔ blocked-on-1.4 with those decisions recorded; its JSONB single-row schema sketch is superseded by O2's normalized shape (convert at build time). Then wrote `docs/devplans/1.4-accounts-login.md`: `accounts` table (BIGSERIAL id, unique lower-cased username, **PHC-style `pbkdf2$iter$salt$hash`** via built-in `Rfc2898DeriveBytes` — no new NuGet), `AccountAuthenticator : NetworkAuthenticator` doing an **async credential check off the main thread via 1.2's `LoadAsync`** during the connect handshake (register = `INSERT … ON CONFLICT (username) DO NOTHING RETURNING`; login = SELECT + constant-time `Verify` + touch `last_login_at`), account id + session token stashed in `conn.authenticationData` (the seam 1.3/1.5 read at spawn), single-login map, pre-connect `LoginUI`, host auto-login via seeded `dev` account. Transport stays unencrypted (KCP) — hardened auth/encryption explicitly deferred to 5.3. **User accepted all O1–O5 recommendations** (in-client register / PBKDF2 / in-memory token / reject-if-online / seed dev account) → **implemented 1.4 this session.** New: `0004_accounts.sql` (`accounts` table — BIGSERIAL id, unique username, PHC `password_hash`; also `DROP TABLE dal_smoke` retiring the 1.2 smoke table); `Assets/Scripts/Server/Auth/PasswordHasher.cs` (PBKDF2 via `Rfc2898DeriveBytes` SHA-256, 100k iters, `pbkdf2$iter$salt$subkey`, constant-time `Verify`); `Assets/Scripts/Server/Database/AccountRepository.cs` (`TryRegister` ON CONFLICT DO NOTHING RETURNING / `FindByUsername` / `TouchLogin`); `Assets/Scripts/Server/Auth/AccountAuthenticator.cs` (`NetworkAuthenticator`; `AuthRequestMessage`/`AuthResponseMessage`/`AuthCode`; **async credential check via `PersistenceService.LoadAsync` off the main thread**, resolve on marshaled callback; `_pending` + `_onlineByAccount` single-login map; stashes `AccountSession{AccountId,Username,Token}` in `conn.authenticationData`; client sends creds in `OnClientAuthenticate`, handles response); `Assets/Scripts/UI/LoginUI.cs` (IMGUI pre-connect panel — username/password/Login/Register + Host-as-dev + Server Only). Modified: `DatabaseSeeder` seeds idempotent `dev`/`devpass` account (O5); `GameNetworkHUD` trimmed to connected status/Stop only (LoginUI owns pre-connect); `GameNetworkManager.OnServerDisconnect` → `HandleServerDisconnect` (frees single-login), `OnServerAddPlayer` logs the mapped account id (the seam 1.3/1.5 consume); `SceneSetup.CreateNetworkManager` adds `LoginUI` + `AccountAuthenticator` and assigns `nm.authenticator`; `DatabaseTools` gains `Tools/Database/Create Account` (CreateAccountWindow). Transport stays unencrypted (KCP) — 5.3 hardens. Devplan `docs/devplans/1.4-accounts-login.md` status 🟢 Approved. **NOT compiled/tested — Unity editor needed. ⚠️ Existing SampleScene NetworkManager predates this and has no LoginUI/authenticator + the old HUD's connect buttons are gone → must add `LoginUI` + `AccountAuthenticator` to the NetworkManager GO and set `authenticator`, OR re-run `Tools/Setup All`. NEXT (user, in editor): wire the scene as above → `Tools/Database/Run Migrations` (applies 0004, drops dal_smoke) → start Host (expect dev account seeded + login accepted + `[Server] Player added for account #N (dev)`) → test Register/Login/wrong-password/duplicate/already-online from a 2nd client. `roadmap.md` ordering still NOT changed — update (1.4 before 1.3) once verified.** Then implement 1.3.
- 2026-06-21 (cont.): User verified 1.4 register/login works in-editor (option #2 manual wiring), but re-running `Tools/Setup All` to wire the scene **re-added the prototype grey Ground plane + Obstacle cubes on top of the hand-placed Synty map**. Fix (user chose: decouple terrain from Setup All + nav uses Synty colliders so grey Ground can go): `SceneSetup.SetupScene` no longer destroys/creates `Ground`/`Obstacles` — it now only rebuilds the `NetworkManager` GameObject (re-wires transport/authenticator/UI); `CreateGround`+`CreateObstacles` moved behind a new `[MenuItem("Tools/Create Prototype Terrain")]` for fresh/empty scenes only. So Setup All is now safe to re-run against the Synty world. **Resolved:** the grey `Ground` plane carries the scene's `NavMeshSurface`, and the baked navmesh was confirmed (blue gizmo) to **drape over the Synty hills** — i.e. the surface bakes from the Synty terrain geometry and the setup is correct (the baker component just happens to host on the plane; its GameObject ≠ what it bakes). Player-not-sinking (Synty mesh colliders + CharacterController) and mob nav (NavMeshSurface bake) are two independent systems, both working. **Manual cleanup for user:** delete the `Obstacles` GameObject (10 grey cubes) + re-bake (cubes had carved holes); **do NOT delete the grey `Ground`** (would discard the working NavMeshSurface) — just uncheck its `Mesh Renderer` to hide the grey under the Synty meshes. Conceptual teaching point covered: `NavMeshSurface` is a baker (Use Geometry / Collect Objects decide the source), not the floor.
- 2026-06-21 (cont. 2): **1.4 partially verified — single-client login WORKS** (host-as-dev auto-login + register + login all confirmed in-editor). **Multi-client auth cases (wrong password / duplicate username / already-online) NOT yet tested — deferred to next session.** 1.4 is **NOT closed out** and `roadmap.md` ordering is still unchanged (flip to 1.4-before-1.3 + mark ✅ only after multi-client passes). Tried a **standalone build** as the 2nd client → it **crashes instantly** on launch; `Player.log` showed `level0` is **corrupted** (`"The file '…/Ueq_Data/level0' is corrupted! Remove it and launch unity again!"` → `[Position out of bounds!]` → `Crash!!!`), a hard native UnityPlayer crash, **not our code**. A clean rebuild (console clean) reproduced the same corruption — likely environmental (suspect: project under a synced folder mangling build output, or a Unity 6.0.4 build quirk; worth a look someday, parked for now). **Decided to pivot to Multiplayer Play Mode (MPPM)** — no build, virtual players run the project directly. **NEXT SESSION — finish 1.4 multi-client verification via MPPM:** (1) install package `com.unity.multiplayer.playmode` (Package Manager → + → Add by name); (2) `Window → Multiplayer Play Mode`, activate Player 2 (first activation slow; restart editor once if it stalls); (3) ensure Postgres up (`docker compose up -d`); (4) press Play → **main editor** click **Host (dev account)**; (5) in **Player 2** Game view (ONLY use Login/Register on virtual players, never Host/Server Only) run the matrix: `dev`/`devpass`→Login = "already online"; `dev`/wrong→Login = "invalid credentials"; `alice`/`1234`→Register = connects + 2nd char; (after alice exists) `alice`/anything→Register = "username taken"; `alice`/`1234`→Login = success. For strict two-live-clients-same-account: activate Player 3, alice in P2, alice in P3 = "already online", close P2 → P3 can log in. Watch main-editor Console for `[Server] Player added for account #N (username)` (the account→connection seam 1.3 consumes). Once all pass → flip `roadmap.md` (1.4 before 1.3) + mark 1.4 ✅ + devplan log, then implement **1.3** (character-state persistence; decisions already locked: normalized child tables + FK to accounts, `RaceClassRegistry`, persist current HP/mana, drop dal_smoke already done in 0004). **Also still pending from earlier:** delete the `Obstacles` GameObject + re-bake navmesh; hide grey `Ground` MeshRenderer (do NOT delete it — holds the NavMeshSurface).
- 2026-06-20 (cont. 10): 1.2 verified in-editor by user (all console messages as expected). Fixed "referenced script (Unknown) is missing" warnings: root cause was two orphaned `CombatStats` ScriptableObject assets (`Assets/ScriptableObjects/Combat/EnemyCombatStats.asset` + `PlayerCombatStats.asset`) left behind when `CombatStats` was retired (2026-05-05/06) — script deleted but assets remained, referencing dead guid `69b78912…`, unreferenced anywhere. Deleted both + metas + the now-empty `Combat/` folder + `Combat.meta`. (Detection: cross-referenced `m_Script` guids in scenes/prefabs/assets against all `.meta` guids incl. `Library/PackageCache` — the only other "missing" guids are benign third-party: URP `DefaultVolumeProfile.asset` overrides + Synty `*_Convex.asset` collision helper, left as-is.) **1.2 ready to mark ✅; next is 1.3 (character schema + save/load) or M2 content platform.**
- 2026-06-20 (cont. 9): **Implemented roadmap 1.2 — DAL + async save queue** (devplan `docs/devplans/1.2-data-access-layer.md`, approved DA1–DA5 as recommended). New `Assets/Scripts/Server/Database/`: `PersistenceService` (server-only MonoBehaviour singleton; dedicated worker `Thread` draining a `BlockingCollection<ISaveJob>`; **key-based write coalescing** via a `CoalesceMarker` + `_pendingByKey` map so repeat saves of one entity collapse to the latest snapshot; `LoadAsync<T>` runs reads on the thread pool and marshals results to the main thread via a `ConcurrentQueue<Action>` drained in `Update()`; `RunOnMainThread`; `FlushAndStop` drains + joins worker with a 10s cap; bounded write retry 2x w/ backoff then drop+log). `SaveJobs.cs` (`ISaveJob.Execute(conn,tx)`, `IKeyedSaveJob`, `DelegateSaveJob`, `KeyedDelegateSaveJob`). `IRepository.cs` (marker + `DalSmokeSnapshot`/`DalSmokeRepository`; reads take optional `tx` so they work inside a worker job's tx or on a standalone load conn). `Database.RunInTransaction` (conn-supplied + self-opening overloads). Migration `0002_dal_smoke.sql` (throwaway table). `Tools/Database/DAL Self-Test` (play-mode only; exercises plain writes + coalescing + verification read + LoadAsync + cleanup, logs PASS/FAIL). `GameNetworkManager.OnStartServer` creates the service after DB init; new `OnStopServer` flushes + destroys it. Iron rule baked in: worker only touches plain snapshots + Npgsql, never Unity/Mirror objects. Migration + ON CONFLICT upsert/coalesce SQL validated against live DB (rolled back). **NEXT (user, in editor): `Tools/Database/Run Migrations` (applies 0002 → schema_version has 2 rows), start a Host (expect `[DB] PersistenceService started`), run `Tools/Database/DAL Self-Test` (expect write+coalesce PASS, LoadAsync PASS, cleanup done), Stop Host (expect `PersistenceService stopped (queue flushed)`). If green → mark roadmap 1.2 ✅ and move to 1.3 (character schema + save/load round-trip) OR pivot to M2 content platform.**
- 2026-06-20 (cont. 8): **Roadmap 1.1 DONE + verified in-editor.** Host start logged `[DB] Connected to tcp://localhost:5432/ueq` + `[DB] Schema up to date (no pending migrations)` via `GameNetworkManager.OnStartServer` → `Database.InitializeServer()` (the user's earlier `Tools/Database/Run Migrations` had applied `0001`, so the host-start migrate correctly found nothing pending = idempotency proven). Cleanup done: deleted throwaway `Assets/Scripts/Editor/DatabaseSmokeTest.cs` (+meta); marked `roadmap.md` 1.1 ✅ + added to its Devplan log; devplan status → ✅ Done. **Housekeeping DONE:** user removed the orphaned `UEQ_NPGSQL_SMOKETEST` define and confirmed the abort-on-DB-down path (docker stop → host start logs the loud ABORTING error). 1.1 fully verified incl. failure path. **NEXT: 1.2 — server data-access layer + async save queue** (repositories over Npgsql, DB writes off Mirror's tick), or pivot to M2 content platform if the user prefers. Devplan-first: write `docs/devplans/1.2-*.md` before implementing.
- 2026-06-20 (cont. 7): **Npgsql smoke test PASSED** (Unity → Dockerized Postgres 16.13, green dialog) — integration risk for M1 dead. Resolution recap: the full Npgsql 6.0.11 transitive closure Unity doesn't provide had to be listed explicitly in `Assets/packages.config` — System.Text.Json 6.0.10, System.Text.Encodings.Web 6.0.0, System.Threading.Channels 6.0.0, System.Diagnostics.DiagnosticSource 6.0.1, Microsoft.Bcl.AsyncInterfaces 6.0.0 — then NuGet→Restore + reimport/restart. **Built the real 1.1** on top: `Assets/Scripts/Server/Database/` → `DatabaseConfig` (resolves UEQ_DB_CONNSTRING → UEQ_DB_* env → root `db.config.json`; returns `DbSettings{ConnectionString,SeedOnStart}`), `Database` (static facade: `OpenConnection()` factory + `InitializeServer()` connect→migrate→seed, throws to abort), `MigrationRunner` (scans `Assets/StreamingAssets/Database/Migrations/NNNN_*.sql`, bootstraps `schema_version` via IF NOT EXISTS, applies each pending file in a transaction, records version+name, idempotent), `DatabaseSeeder` (no-op stub). First migration `0001_init.sql` (creates `schema_version`). Editor `DatabaseTools.cs` → `Tools/Database/Test Connection | Run Migrations | Reset (drop schema_version)`. Wired `GameNetworkManager.OnStartServer` → `Database.InitializeServer()` in try/catch → loud LogError + `StopServer()` on DB-down. Created root `db.config.json` (gitignored) + `db.config.example.json`. Migration SQL validated against live DB in a rolled-back tx (CREATE + INSERT(version,name) + applied_at default all good; rollback left DB pristine). No asmdefs in project → all compiles into Assembly-CSharp, classes are global (namespace-free to match codebase). **NEXT (user, in editor): run `Tools/Database/Test Connection` (expect PG 16.13 dialog), `Tools/Database/Run Migrations` (expect "Applied 1 migration(s)"; re-run → "No pending migrations"), then start a Host and watch Console for the `[DB] Connected… / Applied… / Seed: nothing` startup sequence; also test the failure path by `docker compose stop` → Host start should log the ABORTING error. Once verified: delete `DatabaseSmokeTest.cs` + remove the `UEQ_NPGSQL_SMOKETEST` define, mark roadmap 1.1 ✅, move to 1.2 (DAL + async save queue).**
- 2026-06-20 (cont. 6): Smoke-test menu `Tools/Database` not appearing after adding the `UEQ_NPGSQL_SMOKETEST` define. Diagnosing: menu only registers if `DatabaseSmokeTest.cs` compiles, which requires (a) the define actually applied on the **active platform** (Scripting Define Symbols are per-platform; must click Apply, not Enter) AND (b) Npgsql loading cleanly (else `using Npgsql;` fails to compile → no menu). Asked user to confirm Console is clear of "Npgsql.dll will not be loaded" errors and free of red compile errors. If Npgsql now loads, fallback option is to drop the `#if` guard so a compile error surfaces explicitly instead of a silently-missing menu.
- 2026-06-19: Auto-attack swing animation verified working in play mode. T-pose/locomotion saga resolved (prefab now has Synty `SM_Chr_*` child with Animator → `PlayerLocomotion.controller` (guid `1f50137d…`) + `PlayerAnimator`, all saved). Confirmed full swing chain is wired end to end: `swing.fbx` (`Assets/Animations/Locomotion/attack/slash/`, Humanoid `animationType: 3`) → controller Attack state (Any-State transition on `Attack` trigger, no exit time, returns at 90%) → `PlayerAutoAttack.RpcPlayAttack()` (ClientRpc, shows for local + remote) → `PlayerAnimator.PlayAttack()` → `SetTrigger("Attack")`. Design note: swing is the ONLY auto-attack anim for now; future work makes auto-attack anim depend on equipped weapon + performer's race/class. Two known cosmetic items deferred: (1) prefab's `PlayerAnimator` was serialized before the `attackTrigger` field existed so it's absent in YAML — falls back to default `"Attack"`, harmless unless renamed; (2) swing clip ~2.27s (68f) full-body vs 2s default weapon delay — fast weapons re-trigger before clip ends (won't restart, `canTransitionToSelf` off); scale Attack-state speed to weapon delay when weapon-specific anims land. **Still imported but NOT wired into the controller**: `Jump.fbx`, `Walking Backwards.fbx`, `Kicking.fbx`, `One Hand Sword Combo.fbx` — deferred (Jump/Backwards = locomotion completion; Kick/Combo = ability anims needing per-ability trigger infra on `PlayerAbilities`).
