// Dev default — swapped for environment.production.ts via angular.json's fileReplacements
// on `ng build --configuration production` (5.11).
export const environment = {
  production: false,
  apiBase: 'http://localhost:5144',
};
