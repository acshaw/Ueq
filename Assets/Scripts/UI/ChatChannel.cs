public enum ChatChannel
{
    Say,
    Shout,
    Whisper,
    System,
    Combat,
    Reward,
    Ability,
    Environment,
    NPC,
    Group,   // 5.3 (GP10) — party chat, zone-independent delivery like every other channel
    Consider // 5.4 (AG1) — the consider mechanic's on-demand faction-standing message
}
